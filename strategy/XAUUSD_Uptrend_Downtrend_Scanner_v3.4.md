{
 "nbformat": 4,
 "nbformat_minor": 5,
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.10.0"
  },
  "colab": {
   "provenance": [],
   "name": "XAUUSD_Uptrend_Scanner.ipynb"
  },
  "title": "XAUUSD Uptrend+Downtrend Scanner v1.0"
 },
 "cells": [
  {
   "cell_type": "markdown",
   "id": "cell_md_header",
   "metadata": {},
   "source": [
    "# XAUUSD Uptrend + Downtrend Scanner — v2.5\nสแกนหาช่วงเวลาที่ 55-bar window เป็นเทรนขึ้น/ลง ตามเงื่อนไข C1–C6\n\n---\n\n**Criteria Uptrend (C1–C6):**\n\n| # | ชื่อ | วัดช่วงไหน | เกณฑ์ |\n|---|------|-----------|-------|\n| C1 | Close position | แท่งล่าสุดแท่งเดียว | Close ≥ 60% ของ R55 |\n| C2 | Recent low | Low ต่ำสุดใน **5** แท่งล่าสุด | ≥ 55% ของ R55 |\n| C3 | Drop from HH | HH ทั้ง window → Close ล่าสุด | Drop ≤ 30% ของ R55 |\n| C4 | Launch impulse | สแกนตลอด 55 แท่ง | Consecutive bullish body ≥ 25% R55 ใน 2–8 แท่ง |\n| C5 | 25-bar lookback | 25 แท่งก่อน window | ไม่มี bearish bar body > **30%** ของ R55 |\n| C6 | ราคาต้น window | C6_START_BARS (5) แท่งแรก + lookback | Close ≤ **40%** ของ R55 จากล่าง |\n\n**Criteria Downtrend (C1D–C6D) — Mirror ของ Uptrend:**\n\n| # | ชื่อ | วัดช่วงไหน | เกณฑ์ |\n|---|------|-----------|-------|\n| C1D | Close position | แท่งล่าสุดแท่งเดียว | Close ≤ 40% ของ R55 (จากบน) |\n| C2D | Recent high | High สูงสุดใน **5** แท่งล่าสุด | ≤ 45% ของ R55 (จากบน) |\n| C3D | Rise from LL | LL ทั้ง window → Close ล่าสุด | Rise ≤ 30% ของ R55 |\n| C4D | Launch impulse | สแกนตลอด 55 แท่ง | Consecutive bearish body ≥ 25% R55 ใน 2–8 แท่ง |\n| C5D | 25-bar lookback | 25 แท่งก่อน window | ไม่มี bullish bar body > **30%** ของ R55 |\n| C6D | ราคาต้น window | C6_START_BARS (5) แท่งแรก + lookback | Close ≥ **60%** ของ R55 จากล่าง |\n\n---\n\n**Entry Uptrend (BUY):** Swing Low ใน zone 65–85% R55 → price แตะ level → BUY  \n**Entry Downtrend (SELL):** Swing High ใน zone 15–35% R55 → price แตะ level → SELL  \n**TP/SL:** Symmetric 50% × (Swing High − Entry) | R:R = 1.0 | Lot = Portfolio×10% ÷ SL(pip)\n\n---\n\n**หมายเหตุ:**  \n- `C4_MIN = 0.25` (ค่าเริ่มต้น) รองรับ continuation phase ที่ R55 เล็กลง  \n- `C5_MAX = 0.30` (30% R55) ไม่ใช่ 35%  \n- `C2` วัด **5 แท่ง** ล่าสุด ไม่ใช่ 10 แท่ง  \n- Anti-trend filter: detect_down/up_father (40–100% R55 ใน 1–8 แท่ง)\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "cell_mount",
   "metadata": {},
   "outputs": [],
   "source": [
    "# ============================================================\n# Cell 2 — Google Auth + Mount Google Drive\n# ============================================================\nimport subprocess, sys\n\ndef _install(pkg):\n    subprocess.check_call([sys.executable, '-m', 'pip', 'install', '-q', pkg])\n\n# ── 1. ติดตั้ง Package ────────────────────────────────────\nprint('📦 ติดตั้ง google-api-python-client ...')\n_install('google-api-python-client')\n_install('google-auth-httplib2')\n_install('google-auth-oauthlib')\nprint('✅ ติดตั้งสำเร็จ')\n\n# ── 2. Google Auth พร้อม scope Sheets + Drive ─────────────\nfrom google.colab import auth as _colab_auth\n_colab_auth.authenticate_user()\n\nfrom google.auth import default as _gauth_default\n_GSHEET_CREDS, _ = _gauth_default(scopes=[\n    'https://www.googleapis.com/auth/spreadsheets',\n    'https://www.googleapis.com/auth/drive',\n])\nprint('✅ Google Auth สำเร็จ (Sheets + Drive scope)')\n\n# ── 3. Mount Google Drive ─────────────────────────────────\nfrom google.colab import drive\ndrive.mount('/content/drive')\nprint('✅ Mount สำเร็จ — พร้อมใช้งาน')\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "cell_functions",
   "metadata": {},
   "outputs": [],
   "source": [
    "# ============================================================\n# Cell 3 — Functions (Trend + Entry Scanner)\n# ============================================================\n\nimport pandas as pd\nimport numpy as np\n\n\n# ─── Load CSV ────────────────────────────────────────────────\ndef load_data(csv_path: str) -> pd.DataFrame:\n    \"\"\"\n    โหลด OHLC CSV → DataFrame\n    รองรับ: มี/ไม่มี header, มี/ไม่มี volume column\n    format: time, open, high, low, close [, volume, ...]\n    \"\"\"\n    raw = pd.read_csv(csv_path, header=None, dtype=str)\n\n    # ตรวจ header: ถ้าแถวแรก col[1] แปลงเป็น float ไม่ได้ = มี header\n    try:\n        float(str(raw.iloc[0, 1]).strip())\n    except (ValueError, AttributeError):\n        raw = raw.iloc[1:].reset_index(drop=True)\n\n    # ตั้งชื่อ column\n    base = ['time', 'open', 'high', 'low', 'close']\n    extra = [f'_x{i}' for i in range(raw.shape[1] - len(base))]\n    raw.columns = (base + extra)[:raw.shape[1]]\n\n    # แปลงชนิดข้อมูล\n    raw['time'] = pd.to_datetime(raw['time'].str.strip())\n    for c in ['open', 'high', 'low', 'close']:\n        raw[c] = pd.to_numeric(raw[c].str.strip(), errors='coerce')\n\n    df = (raw.dropna(subset=['time', 'open', 'high', 'low', 'close'])\n            .sort_values('time')\n            .reset_index(drop=True)\n         [['time', 'open', 'high', 'low', 'close']])\n    return df\n\n\n# ─── C1: Close position ──────────────────────────────────────\ndef check_c1(w: pd.DataFrame, L55: float, R55: float) -> tuple:\n    \"\"\"C1: Close แท่งล่าสุด >= C1_MIN ของ R55\"\"\"\n    val = (w['close'].iloc[-1] - L55) / R55\n    return val, val >= C1_MIN\n\n\n# ─── C2: Recent low ──────────────────────────────────────────\ndef check_c2(w: pd.DataFrame, L55: float, R55: float, n_bars: int = 5) -> tuple:\n    \"\"\"C2: Low ต่ำสุดใน 10 แท่งล่าสุด >= C2_MIN ของ R55\"\"\"\n    val = (w['low'].iloc[-n_bars:].min() - L55) / R55\n    return val, val >= C2_MIN\n\n\n# ─── C3: Drop from HH ────────────────────────────────────────\ndef check_c3(w: pd.DataFrame, R55: float) -> tuple:\n    \"\"\"C3: Drop จาก HH -> Close ล่าสุด <= C3_MAX ของ R55\"\"\"\n    val = (w['high'].max() - w['close'].iloc[-1]) / R55\n    return val, val <= C3_MAX\n\n\n# ─── C4: Launch impulse ──────────────────────────────────────\ndef check_c4(w: pd.DataFrame, R55: float) -> tuple:\n    \"\"\"\n    C4: หา consecutive bullish bars ที่ body รวม >= C4_MIN * R55 ใน 2-8 แท่ง\n    ต้องเป็น bullish (close > open) ทุกแท่งในลำดับ\n    คืน (best_ratio, pass)\n    \"\"\"\n    op   = w['open'].values\n    cl   = w['close'].values\n    n    = len(op)\n    best = 0.0\n\n    for start in range(n):\n        bsum = 0.0\n        cnt  = 0\n        for end in range(start, min(start + 8, n)):\n            body = cl[end] - op[end]\n            if body <= 0:\n                break                       # ไม่ bullish → หยุด\n            bsum += body\n            cnt  += 1\n            if cnt >= 2:\n                ratio = bsum / R55\n                if ratio > best:\n                    best = ratio\n                if ratio >= C4_MIN:\n                    return best, True       # พบแล้ว → คืนเลย\n\n    return best, False\n\n\n# ─── C5: 25-bar lookback ─────────────────────────────────────\ndef check_c5(lb, R55: float) -> tuple:\n    \"\"\"\n    C5: ดู 25 แท่งก่อน window\n    Fail ถ้าพบแท่ง Bearish body แท่งเดียว > C5_MAX * R55\n    (R55 = Range ของ window 55 แท่งหลัก)\n    คืน (worst_ratio, pass, label)\n      label: 'uptrend' | 'sideways' | 'slight_down' | 'bearish_drop' | 'n/a'\n    \"\"\"\n    if lb is None or len(lb) < 5:\n        return 0.0, True, 'n/a'\n\n    if R55 < 1e-6:\n        return 0.0, True, 'flat'\n\n    op    = lb['open'].values\n    cl    = lb['close'].values\n    n     = len(op)\n    worst = 0.0\n\n    for i in range(n):\n        body  = op[i] - cl[i]          # bearish = positive\n        if body <= 0:\n            continue\n        ratio = body / R55\n        if ratio > worst:\n            worst = ratio\n        if ratio >= C5_MAX:\n            return worst, False, 'bearish_drop'\n\n    # จัดประเภท lookback\n    H25 = lb['high'].max(); L25 = lb['low'].min(); R25 = H25 - L25\n    net = (lb['close'].iloc[-1] - lb['close'].iloc[0]) / R25 if R25 > 1e-6 else 0\n    if net > 0.15:    lbl = 'uptrend'\n    elif net > -0.05: lbl = 'sideways'\n    else:             lbl = 'slight_down'\n\n    return worst, True, lbl\n\n\n# ─── C6: Start zone ──────────────────────────────────────────\ndef check_c6(w: pd.DataFrame, lb, L55: float, R55: float) -> tuple:\n    \"\"\"\n    C6: ราคาใน lookback 25 แท่ง + ต้น window C6_START_BARS แท่งแรก\n        ต้องไม่มี Close เกิน L55 + C6_MAX * R55\n    คืน (max_pct, pass)\n    \"\"\"\n    level = L55 + C6_MAX_START * R55\n\n    start_closes = w['close'].iloc[:C6_START_BARS]\n\n    if lb is not None and len(lb) > 0:\n        all_closes = pd.concat([lb['close'], start_closes])\n    else:\n        all_closes = start_closes\n\n    max_close = all_closes.max()\n    max_pct   = (max_close - L55) / R55\n    return max_pct, max_close <= level\n\n\n# ─── Main Scanner ─────────────────────────────────────────────\ndef scan_uptrend(df: pd.DataFrame) -> list:\n    \"\"\"\n    เลื่อน WINDOW-bar window ทีละ 1 แท่ง\n    แสดง live progress ระหว่างสแกน\n    คืน list of dict สำหรับทุก window ที่ C1-C5 ผ่านครบ\n    \"\"\"\n    from IPython.display import clear_output\n\n    n        = len(df)\n    total_w  = n - WINDOW + 1\n    results  = []\n\n    # อัปเดต display ทุก UPDATE_EVERY windows (หรือน้อยกว่าถ้า dataset เล็ก)\n    UPDATE_EVERY = max(1, min(200, total_w // 100))\n\n    # ── helper: progress bar ─────────────────────────────────\n    def _bar(done, total, width=28):\n        filled = int(width * done / total)\n        return '█' * filled + '░' * (width - filled)\n\n    # ── helper: นับจำนวน runs (เช็ค bar_idx + time gap) ─────\n    def _runs():\n        if not results:\n            return 0\n        cnt = 1\n        for j in range(1, len(results)):\n            bar_ok  = (results[j]['bar_idx'] == results[j-1]['bar_idx'] + 1)\n            gap_min = (results[j]['ws'] - results[j-1]['ws']).total_seconds() / 60\n            time_ok = gap_min <= TIMEFRAME_MIN * 2\n            if not (bar_ok and time_ok):\n                cnt += 1\n        return cnt\n\n    # ── helper: แสดง progress ────────────────────────────────\n    def _show(done, w_slice, crit=None):\n        pct  = done / total_w * 100\n        t0   = w_slice['time'].iloc[0].strftime('%d/%m/%Y %H:%M')\n        t1   = w_slice['time'].iloc[-1].strftime('%d/%m/%Y %H:%M')\n\n        clear_output(wait=True)\n\n        print(f\"{'═'*64}\")\n        print(f\"  XAUUSD Uptrend Scanner — กำลังสแกน...\")\n        print(f\"{'═'*64}\")\n\n        # Progress bar\n        print(f\"  [{_bar(done, total_w)}] {pct:5.1f}%\")\n        print(f\"  สแกนแล้ว : {done:,} / {total_w:,} windows\")\n        print()\n\n        # Window ปัจจุบัน\n        print(f\"  Window ปัจจุบัน : {t0}  →  {t1}\")\n\n        if crit is not None:\n            v1, v2, v3, v4, c5l, v6, p6, passed = crit\n\n            def _s(val, thr, mode='ge'):\n                ok = (val >= thr) if mode == 'ge' else (val <= thr)\n                return f\"{val:.0%}{'✓' if ok else '✗'}\"\n\n            c5ok = c5l != 'bearish_drop'\n            print(f\"  Criteria        : \"\n                  f\"C1:{_s(v1,C1_MIN)}  \"\n                  f\"C2:{_s(v2,C2_MIN)}  \"\n                  f\"C3:{_s(v3,C3_MAX,'le')}  \"\n                  f\"C4:{_s(v4,C4_MIN)}  \"\n                  f\"C5:{c5l}{'✓' if c5ok else '✗'}  \"\n                  f\"C6:{v6:.0%}{'✓' if p6 else '✗'}\")\n            status = '🟢 ผ่านทุกข้อ — บันทึกแล้ว' if passed else '⬜ ไม่ผ่าน'\n            print(f\"  ผล              : {status}\")\n\n        # สรุปที่พบแล้ว\n        print()\n        print('─'*64)\n        rc = _runs()\n        if results:\n            last = results[-1]\n            print(f\"  พบเทรนขึ้น : {len(results):,} windows ใน {rc} ช่วง\")\n            print(f\"  ล่าสุด     : \"\n                  f\"{last['ws'].strftime('%d/%m/%Y %H:%M')} → \"\n                  f\"{last['we'].strftime('%d/%m/%Y %H:%M')}  \"\n                  f\"[R55={last['R55_pip']:,}p \"\n                  f\"C1={last['c1']:.0%} C2={last['c2']:.0%} \"\n                  f\"C3={last['c3']:.0%} C4={last['c4']:.0%}]\")\n        else:\n            print(f\"  ยังไม่พบเทรนขึ้น\")\n        print(f\"{'═'*64}\")\n\n    # ── Initial display ──────────────────────────────────────\n    _show(0, df.iloc[:WINDOW])\n\n    # ── Main loop ────────────────────────────────────────────\n    for i in range(WINDOW, n + 1):\n        w   = df.iloc[i - WINDOW : i]\n        H55 = w['high'].max()\n        L55 = w['low'].min()\n        R55 = H55 - L55\n        done = i - WINDOW + 1\n\n        if R55 < MIN_R55:\n            if done % UPDATE_EVERY == 0:\n                _show(done, w)\n            continue\n\n        # Lookback\n        lb_end   = i - WINDOW\n        lb_start = max(0, lb_end - LOOKBACK)\n        lb = df.iloc[lb_start:lb_end] if lb_end > lb_start else None\n\n        # Check criteria\n        v1, p1           = check_c1(w, L55, R55)\n        v2, p2           = check_c2(w, L55, R55)\n        v3, p3           = check_c3(w, R55)\n        v4, p4           = check_c4(w, R55)\n        v5, p5, c5_label = check_c5(lb, R55)\n        v6, p6           = check_c6(w, lb, L55, R55)\n\n        passed = p1 and p2 and p3 and p4 and p5 and p6\n\n        if passed:\n            results.append({\n                'bar_idx' : i,\n                'ws'      : w['time'].iloc[0],\n                'we'      : w['time'].iloc[-1],\n                'R55_pip' : int(round(R55 * 100)),\n                'c1' : round(v1, 4),\n                'c2' : round(v2, 4),\n                'c3' : round(v3, 4),\n                'c4' : round(v4, 4),\n                'c5' : c5_label,\n                'c6' : round(v6, 4),\n            })\n\n        # อัปเดต display: ทุก UPDATE_EVERY window, ทุก window สุดท้าย,\n        #                  หรือทันทีที่พบเทรน\n        if done % UPDATE_EVERY == 0 or done == total_w or passed:\n            _show(done, w, (v1, v2, v3, v4, c5_label, v6, p6, passed))\n\n    # ── Final summary display ────────────────────────────────\n    print(f\"{'═'*64}\")\n    print(f\"  ✅ Uptrend scan เสร็จสิ้น\")\n    print(f\"{'═'*64}\")\n    print(f\"  สแกน    : {total_w:,} windows จาก {n:,} แท่ง\")\n    print(f\"  พบ      : {len(results):,} windows ผ่าน criteria\")\n    print(f\"  ช่วงเทรน : {_runs()} ช่วง\")\n    print(f\"{'═'*64}\\n\")\n    return results\n\n\n\n# ─── Display Results ──────────────────────────────────────────\ndef simulate_trades(entries: list, df: pd.DataFrame, portfolio: float = 1000.0) -> list:\n    \"\"\"\n    จำลองการเทรด BUY bar-by-bar\n    - TP = entry + 80% × (highest swing high body_hi ใน window ณ จุดเข้า)\n    - SL = entry − (TP − entry)   [symmetric]\n    - 1 ไม้ต่อครั้ง: ไม้ใหม่เปิดได้หลัง TP/SL โดนเท่านั้น\n    Returns: list of trade dicts\n    \"\"\"\n    if not entries:\n        return []\n\n    WINDOW = 55\n    bars = [\n        (int(i + 1), row['time'].strftime('%Y-%m-%d %H:%M'),\n         row['open'], row['high'], row['low'], row['close'])\n        for i, row in df.iterrows()\n    ]\n    n = len(bars)\n\n    def get_sh_body_hi(bar_i):\n        \"\"\"หา highest swing high body_hi ใน window ณ bar_i\"\"\"\n        w_start = max(0, bar_i - WINDOW + 1)\n        w_bars  = bars[w_start: bar_i + 1]\n        if len(w_bars) < 4:\n            return None\n        H55  = max(b[3] for b in w_bars)\n        L55  = min(b[4] for b in w_bars)\n        R55  = (H55 - L55) * 100\n        if R55 < 1.0:\n            return None\n        highs, _ = scan_swings(w_bars, R55)\n        if not highs:\n            return None\n        return max(h['body_hi'] for h in highs)\n\n    from IPython.display import clear_output\n    trades          = []\n    open_trade      = None\n    stop_segments   = set()   # segment_id ที่แพ้แล้ว → ข้าม\n\n    sorted_entries = sorted(entries, key=lambda e: e['entry_bar_i'])\n    entry_ptr = 0\n    SIM_UPDATE = max(1, n // 50)\n\n    for bi in range(n):\n        cur = bars[bi]\n\n        # ── progress ──\n        if bi % SIM_UPDATE == 0 or bi == n - 1:\n            pct = (bi + 1) / n * 100\n            filled = int(28 * (bi + 1) / n)\n            bar = '█' * filled + '░' * (28 - filled)\n            clear_output(wait=True)\n            wins_n  = sum(1 for t in trades if t['result'] == 'WIN')\n            losses_n = sum(1 for t in trades if t['result'] == 'LOSS')\n            print('═' * 64)\n            print('  📈 BUY Simulate Trades')\n            print('═' * 64)\n            print(f'  [{bar}] {pct:5.1f}%  ({bi+1:,}/{n:,} bars)')\n            print(f'  Trade ปิดแล้ว: {wins_n+losses_n}  '\n                  f'✅{wins_n} ❌{losses_n}')\n            print('═' * 64)\n\n        # ── ตรวจ TP/SL ถ้ามีไม้เปิดอยู่ ─────────────────────\n        if open_trade:\n            tp = open_trade['tp']\n            sl = open_trade['sl']\n            # TP โดน (high >= tp)\n            if cur[3] >= tp:\n                pip = round((tp - open_trade['entry']) * 100, 1)\n                usd_win = round(pip * open_trade['lot'], 2)\n                trades.append({**open_trade,\n                               'result'    : 'WIN',\n                               'close_time': cur[1][5:16],\n                               'close_price': tp,\n                               'pip'       : pip,\n                               'usd'       : usd_win})\n                open_trade = None\n            # SL โดน (low <= sl)\n            elif cur[4] <= sl:\n                pip = round((sl - open_trade['entry']) * 100, 1)\n                usd_loss = round(pip * open_trade['lot'], 2)\n                # บันทึก segment นี้ว่าแพ้ → หยุดเทรดใน segment นี้\n                _seg = open_trade.get('segment_id')\n                if _seg is not None:\n                    stop_segments.add(_seg)\n                trades.append({**open_trade,\n                               'result'    : 'LOSS',\n                               'close_time': cur[1][5:16],\n                               'close_price': sl,\n                               'pip'       : pip,\n                               'usd'       : usd_loss})\n                open_trade = None\n\n        # ── เปิดไม้ใหม่ถ้าไม่มีไม้ค้างอยู่ ──────────────────\n        if open_trade is None:\n            while entry_ptr < len(sorted_entries):\n                e = sorted_entries[entry_ptr]\n                if e['entry_bar_i'] == bi:\n                    entry_ptr += 1\n                    # ข้าม entry ถ้า segment นี้แพ้ไปแล้ว\n                    if e.get('segment_id') in stop_segments:\n                        continue\n                    sh_hi = get_sh_body_hi(bi)\n                    if sh_hi is None or sh_hi <= e['entry_price']:\n                        continue\n                    reward = sh_hi - e['entry_price']\n                    tp_dist = 0.50 * reward  # ← เปลี่ยน % ที่นี่\n                    tp      = round(e['entry_price'] + tp_dist, 3)\n                    sl      = round(e['entry_price'] - tp_dist, 3)  # symmetric\n                    risk_amt = portfolio * 0.10\n                    sl_pip   = round((e['entry_price'] - sl) * 100, 1)\n                    tp_pip   = round((tp - e['entry_price']) * 100, 1)\n                    lot      = round(risk_amt / sl_pip, 4) if sl_pip > 0 else 0\n                    rr       = round(tp_pip / sl_pip, 2) if sl_pip > 0 else 0\n                    open_trade = {\n                        'entry_time' : e['entry_time'],\n                        'entry'      : e['entry_price'],\n                        'tp'         : tp,\n                        'sl'         : sl,\n                        'ref'        : e['sl_b1_time'],\n                        'sh_hi'      : round(sh_hi, 3),\n                        'reward_pip' : round(reward * 100, 1),\n                        'lot'        : lot,\n                        'rr'         : rr,\n                        'risk_usd'   : round(risk_amt, 2),\n                        'segment_id' : e.get('segment_id'),\n                    }\n                    break\n                elif e['entry_bar_i'] < bi:\n                    entry_ptr += 1\n                else:\n                    break\n\n    # ไม้ที่ยังเปิดอยู่ตอนจบข้อมูล\n    if open_trade:\n        last = bars[-1]\n        open_pip = round((last[5] - open_trade['entry']) * 100, 1)\n        trades.append({**open_trade,\n                       'result'     : 'OPEN',\n                       'close_time' : last[1][5:16],\n                       'close_price': last[5],\n                       'pip'        : open_pip,\n                       'usd'        : round(open_pip * open_trade['lot'], 2)})\n    clear_output(wait=True)\n    wins_f = sum(1 for t in trades if t['result']=='WIN')\n    losses_f = sum(1 for t in trades if t['result']=='LOSS')\n    print(f'  ✅ BUY Simulate เสร็จ: {wins_f}W/{losses_f}L')\n    return trades\n\n\n# ╔══════════════════════════════════════════════════════════════╗\n# ║              DOWNTREND SCANNER (Mirror of Uptrend)          ║\n# ╚══════════════════════════════════════════════════════════════╝\n\n# ─── C1D: Close แท่งล่าสุด <= C1_MIN ของ R55 (จากบน) ─────────\n\ndef check_c1d(w: pd.DataFrame, H55: float, R55: float) -> tuple:\n    \"\"\"C1D: Close แท่งล่าสุด <= (1-C1_MIN) ของ R55 จากบน\"\"\"\n    val = (H55 - w['close'].iloc[-1]) / R55\n    return val, val >= C1_MIN\n\n# ─── C2D: Recent high ─────────────────────────────────────────\n\ndef check_c2d(w: pd.DataFrame, H55: float, R55: float, n_bars: int = 5) -> tuple:\n    \"\"\"C2D: High สูงสุดใน 5 แท่งล่าสุด <= C2_MIN ของ R55 จากบน\"\"\"\n    val = (H55 - w['high'].iloc[-n_bars:].max()) / R55\n    return val, val >= C2_MIN\n\n# ─── C3D: Rise from LL ────────────────────────────────────────\n\ndef check_c3d(w: pd.DataFrame, R55: float) -> tuple:\n    \"\"\"C3D: Rise จาก LL -> Close ล่าสุด <= C3_MAX ของ R55\"\"\"\n    val = (w['close'].iloc[-1] - w['low'].min()) / R55\n    return val, val <= C3_MAX\n\n# ─── C4D: Launch impulse down ─────────────────────────────────\n\ndef check_c4d(w: pd.DataFrame, R55: float) -> tuple:\n    \"\"\"\n    C4D: หา consecutive bearish bars ที่ body รวม >= C4_MIN * R55\n    (mirror C4 แต่เป็น bearish)\n    \"\"\"\n    op   = w['open'].values\n    cl   = w['close'].values\n    n    = len(op)\n    best = 0.0\n\n    for start in range(n):\n        bsum = 0.0\n        cnt  = 0\n        for end in range(start, min(start + 8, n)):\n            if cl[end] < op[end]:  # bearish\n                bsum += (op[end] - cl[end]) * 100\n                cnt  += 1\n                if 2 <= cnt <= 8:\n                    ratio = bsum / (R55 * 100)\n                    best  = max(best, ratio)\n            else:\n                break\n\n    return best, best >= C4_MIN\n\n# ─── C5D: Lookback bearish continuity ────────────────────────\n\ndef check_c5d(lb, R55: float) -> tuple:\n    \"\"\"\n    C5D: ดู 25 แท่งก่อน window\n    Fail ถ้าพบแท่ง Bullish body แท่งเดียว > C5_MAX * R55\n    \"\"\"\n    if lb is None or len(lb) < 5:\n        return 0.0, True, 'n/a'\n    if R55 < 1e-6:\n        return 0.0, True, 'n/a'\n\n    worst = 0.0\n    for _, row in lb.iterrows():\n        body = (row['close'] - row['open']) * 100  # bullish ถ้า > 0\n        if body > 0:\n            ratio = body / (R55 * 100)\n            worst = max(worst, ratio)\n\n    label = 'downtrend' if worst < 0.10 else             'slight_up' if worst < C5_MAX else 'bullish_spike'\n    return worst, worst <= C5_MAX, label\n\n# ─── C6D: ราคาต้นไม่สูงเกิน ──────────────────────────────────\n\ndef check_c6d(w: pd.DataFrame, lb, H55: float, R55: float) -> tuple:\n    \"\"\"\n    C6D: ราคาใน lookback + ต้น window ต้องไม่มี Close เกิน\n         H55 - C6_MAX_START * R55\n    \"\"\"\n    level = H55 - C6_MAX_START * R55\n\n    start_closes = w['close'].iloc[:C6_START_BARS]\n\n    if lb is not None and len(lb) > 0:\n        all_closes = pd.concat([lb['close'], start_closes])\n    else:\n        all_closes = start_closes\n\n    if len(all_closes) == 0:\n        return 0.0, True\n\n    max_pct = (all_closes.max() - (H55 - R55)) / R55\n    passed  = all_closes.max() <= level\n    return max_pct, passed\n\n# ─── scan_downtrend ───────────────────────────────────────────\n\ndef scan_downtrend(df: pd.DataFrame) -> list:\n    \"\"\"\n    เลื่อน WINDOW-bar window ทีละ 1 แท่ง\n    แสดง live progress ระหว่างสแกน (Mirror ของ scan_uptrend)\n    คืน list of dict สำหรับทุก window ที่ C1D-C6D ผ่านครบ\n    \"\"\"\n    from IPython.display import clear_output\n\n    n        = len(df)\n    total_w  = n - WINDOW + 1\n    results  = []\n\n    UPDATE_EVERY = max(1, min(200, total_w // 100))\n\n    def _bar(done, total, width=28):\n        filled = int(width * done / total)\n        return '█' * filled + '░' * (width - filled)\n\n    def _runs():\n        if not results: return 0\n        cnt = 1\n        for j in range(1, len(results)):\n            bar_ok  = (results[j]['bar_idx'] == results[j-1]['bar_idx'] + 1)\n            gap_min = (results[j]['ws'] - results[j-1]['ws']).total_seconds() / 60\n            time_ok = gap_min <= TIMEFRAME_MIN * 2\n            if not (bar_ok and time_ok):\n                cnt += 1\n        return cnt\n\n    def _show(done, w_slice, crit=None):\n        pct = done / total_w * 100\n        t0  = w_slice['time'].iloc[0].strftime('%d/%m/%Y %H:%M')\n        t1  = w_slice['time'].iloc[-1].strftime('%d/%m/%Y %H:%M')\n\n        clear_output(wait=True)\n\n        print(f\"{'═'*64}\")\n        print(f\"  XAUUSD Downtrend Scanner — กำลังสแกน...\")\n        print(f\"{'═'*64}\")\n\n        print(f\"  [{_bar(done, total_w)}] {pct:5.1f}%\")\n        print(f\"  สแกนแล้ว : {done:,} / {total_w:,} windows\")\n        print()\n\n        print(f\"  Window ปัจจุบัน : {t0}  →  {t1}\")\n\n        if crit is not None:\n            v1, v2, v3, v4, c5l, v6, p6, passed = crit\n\n            def _s(val, thr, mode='ge'):\n                ok = (val >= thr) if mode == 'ge' else (val <= thr)\n                return f\"{val:.0%}{'✓' if ok else '✗'}\"\n\n            c5ok = c5l != 'bullish_spike'\n            print(f\"  Criteria        : \"\n                  f\"C1:{_s(v1,C1_MIN)}  \"\n                  f\"C2:{_s(v2,C2_MIN)}  \"\n                  f\"C3:{_s(v3,C3_MAX,'le')}  \"\n                  f\"C4:{_s(v4,C4_MIN)}  \"\n                  f\"C5:{c5l}{'✓' if c5ok else '✗'}  \"\n                  f\"C6:{v6:.0%}{'✓' if p6 else '✗'}\")\n            status = '🔴 ผ่านทุกข้อ — บันทึกแล้ว' if passed else '⬜ ไม่ผ่าน'\n            print(f\"  ผล              : {status}\")\n\n        print()\n        print('─'*64)\n        rc = _runs()\n        if results:\n            last = results[-1]\n            print(f\"  พบเทรนลง : {len(results):,} windows ใน {rc} ช่วง\")\n            print(f\"  ล่าสุด    : \"\n                  f\"{last['ws'].strftime('%d/%m/%Y %H:%M')} → \"\n                  f\"{last['we'].strftime('%d/%m/%Y %H:%M')}  \"\n                  f\"[R55={last['R55_pip']:,}p \"\n                  f\"C1={last['c1']:.0%} C2={last['c2']:.0%} \"\n                  f\"C3={last['c3']:.0%} C4={last['c4']:.0%}]\")\n        else:\n            print(f\"  ยังไม่พบเทรนลง\")\n        print(f\"{'═'*64}\")\n\n    _show(0, df.iloc[:WINDOW])\n\n    for idx in range(total_w):\n        i  = idx + WINDOW\n        w  = df.iloc[idx:i]\n        lb_e = idx; lb_s = max(0, lb_e - LOOKBACK)\n        lb   = df.iloc[lb_s:lb_e] if lb_e > lb_s else None\n\n        H55 = w['high'].max(); L55 = w['low'].min(); R55 = H55 - L55\n        done = idx + 1\n\n        if R55 < MIN_R55:\n            if done % UPDATE_EVERY == 0: _show(done, w)\n            continue\n\n        v1,p1 = check_c1d(w, H55, R55)\n        v2,p2 = check_c2d(w, H55, R55)\n        v3,p3 = check_c3d(w, R55)\n        v4,p4 = check_c4d(w, R55)\n        v5,p5,lbl5 = check_c5d(lb, R55)\n        v6,p6 = check_c6d(w, lb, H55, R55)\n        passed = p1 and p2 and p3 and p4 and p5 and p6\n\n        if passed:\n            results.append({\n                'bar_idx' : i,\n                'ws'  : w['time'].iloc[0],\n                'we'  : w['time'].iloc[-1],\n                'R55_pip' : int(round(R55 * 100)),\n                'H55' : H55, 'L55' : L55,\n                'c1'  : round(v1, 4), 'c2' : round(v2, 4),\n                'c3'  : round(v3, 4), 'c4' : round(v4, 4),\n                'c5'  : lbl5,         'c6' : round(v6, 4),\n            })\n\n        if done % UPDATE_EVERY == 0 or done == total_w or passed:\n            _show(done, w, (v1, v2, v3, v4, lbl5, v6, p6, passed))\n\n    print(f\"{'═'*64}\")\n    print(f\"  ✅ Downtrend scan เสร็จสิ้น\")\n    print(f\"{'═'*64}\")\n    print(f\"  สแกน    : {total_w:,} windows จาก {n:,} แท่ง\")\n    print(f\"  พบ      : {len(results):,} windows ผ่าน criteria\")\n    print(f\"  ช่วงเทรน : {_runs()} ช่วง\")\n    print(f\"{'═'*64}\\n\")\n    return results\n\ndef detect_up_father(bars: list, i: int, R55: float,\n                     max_bars: int = 8,\n                     min_pct: float = 40.0,\n                     max_pct: float = 100.0) -> bool:\n    \"\"\"\n    ตรวจว่า bar ปัจจุบัน (index i) เป็นจุดสิ้นสุดของ แท่งพ่อขึ้น ไหม\n    Mirror ของ detect_down_father\n    \"\"\"\n    if R55 <= 0:\n        return False\n    cur_close = bars[i][5]\n    for n in range(1, max_bars + 1):\n        start = i - n + 1\n        if start < 0:\n            break\n        first_open  = bars[start][2]\n        total_body  = (cur_close - first_open) * 100  # pip (ขึ้นถ้า > 0)\n        pct         = total_body / R55 * 100\n        if min_pct <= pct <= max_pct:\n            return True\n    return False\n\n# ─── scan_entries_bar_by_bar_down ─────────────────────────────\n\ndef scan_entries_bar_by_bar_down(df: pd.DataFrame) -> tuple:\n    \"\"\"\n    Bar-by-bar entry scanner สำหรับ Downtrend (SELL)\n    Mirror ของ scan_entries_bar_by_bar ทุกประการ:\n      - ใช้ Swing High (แทน Swing Low) เป็น REF\n      - Zone: 15-35% จากล่าง (mirror ของ 65-85%)\n      - Entry: price HIGH >= level → SELL\n      - Anti-trend: detect_up_father (แท่งพ่อขึ้น)\n    \"\"\"\n    from IPython.display import clear_output\n    entries        = []\n    all_swing_highs = []\n    last_trend_we     = None\n    last_confirmed_we = None\n    sl_key_times      = {}\n    segment_start_we  = None\n\n    watch_list   = {}   # level → info\n    seen_sh_keys = set()\n\n    bars = [\n        (int(i + 1), row['time'].strftime('%Y-%m-%d %H:%M'),\n         row['open'], row['high'], row['low'], row['close'])\n        for i, row in df.iterrows()\n    ]\n    n = len(df)\n\n    UPDATE_EVERY = max(1, min(500, (n - WINDOW + 1) // 20))\n    total_w      = n - WINDOW + 1\n    done         = 0\n\n    for i in range(WINDOW, n + 1):\n        done += 1\n        w        = df.iloc[i - WINDOW:i]\n        cur_bar  = bars[i - 1]\n        cur_high = cur_bar[3]\n        w_bars   = bars[i - WINDOW:i]\n\n        H55  = w['high'].max()\n        L55  = w['low'].min()\n        R55_ref = (H55 - L55) * 100\n\n        if R55_ref < MIN_R55 * 100:\n            continue\n\n        cur_we = w['time'].iloc[-1]\n        cur_ws = w['time'].iloc[0]\n\n        # ── Anti-trend: detect_up_father ──────────────────────\n        if last_confirmed_we is not None:\n            H55c = max(b[3] for b in w_bars)\n            L55c = min(b[4] for b in w_bars)\n            R55c = (H55c - L55c) * 100\n            if R55c > 0 and detect_up_father(bars, i-1, R55c):\n                last_trend_we     = None\n                last_confirmed_we = None\n                segment_start_we  = None\n                watch_list.clear()\n\n        # ── Criteria C1D-C6D ──────────────────────────────────\n        lb_e = max(0, i - WINDOW)\n        lb_s = max(0, lb_e - LOOKBACK)\n        lb   = df.iloc[lb_s:lb_e] if lb_e > lb_s else None\n\n        p1 = check_c1d(w, H55, H55 - L55)[1]\n        p2 = check_c2d(w, H55, H55 - L55)[1]\n        p3 = check_c3d(w, H55 - L55)[1]\n        p4 = check_c4d(w, H55 - L55)[1]\n        p5 = check_c5d(lb, (H55 - L55))[1]\n        p6 = check_c6d(w, lb, H55, H55 - L55)[1]\n        passed = p1 and p2 and p3 and p4 and p5 and p6\n\n        if passed:\n            _prev_confirmed   = last_confirmed_we\n            last_confirmed_we = cur_we\n            last_trend_we     = cur_we\n            if _prev_confirmed is None or cur_ws >= _prev_confirmed:\n                segment_start_we = cur_we\n        elif last_trend_we is not None and cur_ws < last_trend_we:\n            pass  # is_trend ยังเป็น True (overlap)\n        else:\n            last_trend_we = None\n\n        is_trend = (last_confirmed_we is not None and cur_we <= last_confirmed_we)\n\n        # retroactive cleanup เมื่อ segment ใหม่\n        if passed and last_confirmed_we == cur_we:\n            _prev = last_confirmed_we  # = cur_we แล้ว\n            # ใช้ _prev_confirmed ที่เก็บไว้ก่อนหน้า\n            pass  # cleanup จัดการแล้วใน passed block\n\n        # ── scan Swing High ───────────────────────────────────\n        if is_trend:\n            highs, _ = scan_swings(w_bars, R55_ref)\n            thresh_body = SL_MIN_BODY_PCT * R55_ref\n\n            for sh in highs:\n                min_body = min(\n                    bsize(w_bars[sh['bar_nums'][0] - w_bars[0][0]]),\n                    bsize(w_bars[sh['bar_nums'][1] - w_bars[0][0]])\n                ) * 100\n\n                if min_body <= thresh_body:\n                    continue\n\n                key = tuple(sh['bar_nums'])\n                if key in seen_sh_keys:\n                    continue\n\n                # ── Zone filter: body_hi ต้องอยู่ใน 15-35% จากล่าง ──\n                level  = sh['body_hi']\n                _pct   = (level - L55) / ((H55 - L55) / 100) if (H55 - L55) > 0 else 0\n                _in_zone = 15.0 <= _pct <= 35.0\n\n                seen_sh_keys.add(key)\n                sl_key_times[key] = cur_we\n                all_swing_highs.append({\n                    'body_hi'  : level,\n                    'b1_time'  : sh['times'][0][5:16],\n                    'b2_time'  : sh['times'][1][5:16],\n                    'bar_nums' : sh['bar_nums'],\n                    'thick'    : sh.get('thick', False),\n                    'marked_at': cur_we,\n                    'zone_pct' : _pct,\n                    'in_zone'  : _in_zone,\n                })\n\n                if not _in_zone:\n                    continue\n\n                if level not in watch_list:\n                    watch_list[level] = {\n                        'sl'        : sh,\n                        'sl_b1_time': sh['times'][0][5:16],\n                        'sl_b2_time': sh['times'][1][5:16],\n                        'marked_at' : cur_we,\n                    }\n\n        # ── ลบ swing high ที่หลุดออกจาก window ────────────────\n        win_start_barnum = bars[i - WINDOW][0]\n        for level in list(watch_list.keys()):\n            info = watch_list[level]\n            sh_bar_nums = info['sl']['bar_nums']\n            if max(sh_bar_nums) < win_start_barnum:\n                watch_list.pop(level)\n\n        # ── เช็ค entry: High >= level ─────────────────────────\n        _cur_time2 = w['time'].iloc[-1]\n        _in_conf2  = (last_confirmed_we is not None and\n                      _cur_time2 <= last_confirmed_we)\n        if not _in_conf2:\n            entry_high = None\n        else:\n            entry_high = cur_high\n\n        for level in (list(watch_list.keys()) if entry_high is not None else []):\n            info = watch_list[level]\n            sh_bar_nums = info['sl']['bar_nums']\n            if max(sh_bar_nums) < win_start_barnum:\n                continue\n            if entry_high >= level:\n                entries.append({\n                    'entry_time'  : cur_bar[1][5:16],\n                    'entry_price' : level,\n                    'entry_high'  : entry_high,\n                    'sh_b1_time'  : info['sl_b1_time'],\n                    'sh_b2_time'  : info['sl_b2_time'],\n                    'marked_at'   : info['marked_at'],\n                    'entry_bar_i' : i - 1,\n                    'segment_id'  : segment_start_we,\n                })\n                watch_list.pop(level)\n                break  # 1 entry per bar\n\n        if done % UPDATE_EVERY == 0 or done == total_w:\n            pct = done / total_w * 100\n            filled = int(28 * done / total_w)\n            bar = '█' * filled + '░' * (28 - filled)\n            clear_output(wait=True)\n            print('═' * 64)\n            print('  XAUUSD Downtrend Entry Scanner — Bar-by-Bar')\n            print('═' * 64)\n            print(f'  [{bar}] {pct:5.1f}%')\n            print(f'  สแกนแล้ว : {done:,} / {total_w:,} bars')\n            print(f'  Entry ที่พบ: {len(entries):,} จุด  '\n                  f'Swing High: {len(all_swing_highs):,} จุด')\n            print('═' * 64)\n\n    clear_output(wait=True)\n    print(f'  ✅ Downtrend Entry scan เสร็จ: {len(entries)} entry จุด')\n    return entries, all_swing_highs\n\n# ─── simulate_trades_down ────────────────────────────────────\n\ndef simulate_trades_down(entries: list, df: pd.DataFrame,\n                         portfolio: float = 1000.0) -> list:\n    \"\"\"\n    จำลองการเทรด SELL bar-by-bar (Mirror ของ simulate_trades)\n    - TP = entry - 50% × (entry - lowest swing low body_lo ใน window)\n    - SL symmetric\n    \"\"\"\n    if not entries:\n        return []\n\n    bars = [\n        (int(i + 1), row['time'].strftime('%Y-%m-%d %H:%M'),\n         row['open'], row['high'], row['low'], row['close'])\n        for i, row in df.iterrows()\n    ]\n    n = len(bars)\n\n    def get_sl_body_lo(bar_i):\n        \"\"\"หา lowest swing low body_lo ใน window ณ bar_i\"\"\"\n        w_start = max(0, bar_i - WINDOW + 1)\n        w_b     = bars[w_start: bar_i + 1]\n        if len(w_b) < 4:\n            return None\n        H55 = max(b[3] for b in w_b)\n        L55 = min(b[4] for b in w_b)\n        R55 = (H55 - L55) * 100\n        if R55 < 1.0:\n            return None\n        _, lows = scan_swings(w_b, R55)\n        if not lows:\n            return None\n        return min(lo['body_lo'] for lo in lows)\n\n    from IPython.display import clear_output\n    trades        = []\n    open_trade    = None\n    stop_segments = set()\n\n    sorted_entries = sorted(entries, key=lambda e: e['entry_bar_i'])\n    entry_ptr = 0\n    SIM_UPDATE = max(1, n // 50)\n\n    for bi in range(n):\n        cur = bars[bi]\n\n        # ── progress ──\n        if bi % SIM_UPDATE == 0 or bi == n - 1:\n            pct = (bi + 1) / n * 100\n            filled = int(28 * (bi + 1) / n)\n            bar = '█' * filled + '░' * (28 - filled)\n            clear_output(wait=True)\n            wins_n  = sum(1 for t in trades if t['result'] == 'WIN')\n            losses_n = sum(1 for t in trades if t['result'] == 'LOSS')\n            print('═' * 64)\n            print('  📉 SELL Simulate Trades')\n            print('═' * 64)\n            print(f'  [{bar}] {pct:5.1f}%  ({bi+1:,}/{n:,} bars)')\n            print(f'  Trade ปิดแล้ว: {wins_n+losses_n}  '\n                  f'✅{wins_n} ❌{losses_n}')\n            print('═' * 64)\n\n        if open_trade:\n            tp = open_trade['tp']\n            sl = open_trade['sl']\n            # TP โดน (low <= tp) — SELL TP อยู่ต่ำกว่า\n            if cur[4] <= tp:\n                pip = round((open_trade['entry'] - tp) * 100, 1)\n                usd = round(pip * open_trade['lot'], 2)\n                trades.append({**open_trade,\n                                'result'     : 'WIN',\n                                'close_time' : cur[1][5:16],\n                                'close_price': tp,\n                                'pip'        : pip,\n                                'usd'        : usd})\n                open_trade = None\n            # SL โดน (high >= sl) — SELL SL อยู่สูงกว่า\n            elif cur[3] >= sl:\n                pip = round((open_trade['entry'] - sl) * 100, 1)\n                usd = round(pip * open_trade['lot'], 2)\n                _seg = open_trade.get('segment_id')\n                if _seg is not None:\n                    stop_segments.add(_seg)\n                trades.append({**open_trade,\n                                'result'     : 'LOSS',\n                                'close_time' : cur[1][5:16],\n                                'close_price': sl,\n                                'pip'        : pip,\n                                'usd'        : usd})\n                open_trade = None\n\n        if open_trade is None:\n            while entry_ptr < len(sorted_entries):\n                e = sorted_entries[entry_ptr]\n                if e['entry_bar_i'] == bi:\n                    entry_ptr += 1\n                    if e.get('segment_id') in stop_segments:\n                        continue\n                    sl_lo = get_sl_body_lo(bi)\n                    if sl_lo is None or sl_lo >= e['entry_price']:\n                        continue\n                    reward    = e['entry_price'] - sl_lo\n                    tp_dist   = 0.50 * reward\n                    tp        = round(e['entry_price'] - tp_dist, 3)\n                    sl        = round(e['entry_price'] + tp_dist, 3)\n                    risk_amt  = portfolio * 0.10\n                    sl_pip    = round(tp_dist * 100, 1)\n                    lot       = round(risk_amt / sl_pip, 4) if sl_pip > 0 else 0\n                    rr        = 1.0\n                    open_trade = {\n                        'entry_time' : e['entry_time'],\n                        'entry'      : e['entry_price'],\n                        'tp'         : tp,\n                        'sl'         : sl,\n                        'ref'        : e['sh_b1_time'],\n                        'sl_lo'      : round(sl_lo, 3),\n                        'reward_pip' : round(reward * 100, 1),\n                        'lot'        : lot,\n                        'rr'         : rr,\n                        'risk_usd'   : round(risk_amt, 2),\n                        'segment_id' : e.get('segment_id'),\n                    }\n                    break\n                elif e['entry_bar_i'] < bi:\n                    entry_ptr += 1\n                else:\n                    break\n\n    if open_trade:\n        last = bars[-1]\n        open_pip = round((open_trade['entry'] - last[5]) * 100, 1)\n        trades.append({**open_trade,\n                       'result'     : 'OPEN',\n                       'close_time' : last[1][5:16],\n                       'close_price': last[5],\n                       'pip'        : open_pip,\n                       'usd'        : round(open_pip * open_trade['lot'], 2)})\n    clear_output(wait=True)\n    wins_f = sum(1 for t in trades if t['result']=='WIN')\n    losses_f = sum(1 for t in trades if t['result']=='LOSS')\n    print(f'  ✅ SELL Simulate เสร็จ: {wins_f}W/{losses_f}L')\n    return trades\n\n\ndef display_results(results: list, entries: list = None, swing_lows: list = None, direction: str = 'up') -> None:\n    if not results:\n        print(\"\\n  ❌ ไม่พบ window ที่ผ่านทุก criteria\")\n        return\n\n    SEP = '─' * 63\n    DBL = '═' * 65\n\n    # ── จัดกลุ่ม: non-overlapping + consecutive ─────────────────\n    # กฎ 1: bar_idx ต่อกัน AND time gap ≤ TF*2  → อยู่ใน run เดียวกัน\n    # กฎ 2: ถ้า ws ใหม่ < we ก่อนหน้า = window ยังคร่อมอยู่ = เทรนเดียวกัน → merge\n    #        ถ้า ws ใหม่ >= we ก่อนหน้า = ไม่มี overlap = เทรนใหม่\n    runs     = []\n    run      = []\n    prev_we  = None   # we ล่าสุดของ run ปัจจุบัน\n\n    for r in results:\n        if not run:\n            run     = [r]\n            prev_we = r['we']\n            continue\n\n        if r['ws'] < prev_we:\n            # ws คร่อม we ก่อน → เทรนเดียวกัน ต่อ run\n            run.append(r)\n            prev_we = r['we']\n        else:\n            # ไม่มี overlap → จบ run ปัจจุบัน เริ่มใหม่\n            runs.append(run)\n            run     = [r]\n            prev_we = r['we']\n\n    if run:\n        runs.append(run)\n\n    # นับ windows ที่ถูกข้ามเพราะ overlap\n    total_shown = sum(len(r) for r in runs)\n    skipped     = len(results) - total_shown\n\n    print(\"\\n\" + DBL)\n    print(f\"  ผลการสแกน: {len(results)} windows → {len(runs)} ช่วง{'เทรนขึ้น' if direction=='up' else 'เทรนลง'}\")\n    if skipped:\n        print(f\"  (ข้าม {skipped} windows ที่คาบเกี่ยวกับช่วงก่อนหน้า)\")\n    print(DBL)\n\n    for ri, run in enumerate(runs, 1):\n        n_w = len(run)\n        h, m = divmod(n_w * 5, 60)\n\n        print(f\"\\n  {SEP}\")\n        print(f\"  ► ช่วงที่ {ri}  ({n_w} windows = {h}ชม. {m:02d}นาที)\")\n        print(f\"  {SEP}\")\n\n        for idx, r in enumerate(run):\n            ts = r['ws'].strftime('%d/%m/%Y %H:%M')\n            te = r['we'].strftime('%d/%m/%Y %H:%M')\n\n            if idx == 0:\n                marker = '🟢 เริ่มเทรนขึ้น  ' if direction=='up' else '🔴 เริ่มเทรนลง   '\n            elif idx == n_w - 1:\n                marker = '🔴 สิ้นสุดเทรนขึ้น' if direction=='up' else '🔴 สิ้นสุดเทรนลง '\n            else:\n                marker = '     เป็นเทรนขึ้น ' if direction=='up' else '     เป็นเทรนลง  '\n\n            print(f\"  * {ts} — {te}  {marker}\")\n\n            if VERBOSE and (idx == 0 or idx == n_w - 1):\n                print(f\"      ↳ R55={r['R55_pip']:,}pip  \"\n                      f\"C1={r['c1']:.1%} C2={r['c2']:.1%} \"\n                      f\"C3={r['c3']:.1%} C4={r['c4']:.1%} \"\n                      f\"C5={r['c5']} C6={r['c6']:.1%}\")\n\n        # ── Entry points ในช่วงนี้ ─────────────────────────────\n        if entries:\n            from datetime import datetime as _dt\n            def _p(s):\n                for fmt in ('%d/%m/%Y %H:%M','%m-%d %H:%M','%Y-%m-%d %H:%M'):\n                    try: return _dt.strptime(s.strip(), fmt)\n                    except: pass\n                return None\n            # filter ด้วย marked_at (เวลาที่ swing low ถูก confirm ในเทรน)\n            # ไม่ใช่ entry_time → entry เกิดนอกช่วงเทรนได้\n            rws = run[0]['ws']; rwe = run[-1]['we']\n            rent = []\n            for e in [x for x in entries if x.get('in_trend', True)]:\n                # marked_at = เวลาที่ swing low นี้ถูก confirm ว่าอยู่ในเทรน\n                mat = e.get('marked_at')\n                if mat is None: continue\n                # รองรับทั้ง Timestamp และ string\n                if hasattr(mat, 'to_pydatetime'):\n                    mat = mat.to_pydatetime().replace(tzinfo=None)\n                else:\n                    mat = _p(str(mat))\n                    if mat is None: continue\n                    if mat.year == 1900: mat = mat.replace(year=rws.year)\n                # swing low ต้องอยู่ใน window ของ run นี้\n                if rws <= mat <= rwe:\n                    rent.append(e)\n            # ── Swing Lows ที่พบในช่วงนี้ ─────────────────────\n            if swing_lows:\n                rsls = []\n                for sl in swing_lows:\n                    smat = sl.get('marked_at')\n                    if smat is None: continue\n                    if hasattr(smat, 'to_pydatetime'):\n                        smat = smat.to_pydatetime().replace(tzinfo=None)\n                    else:\n                        smat = _p(str(smat))\n                        if smat is None: continue\n                        if smat.year == 1900: smat = smat.replace(year=rws.year)\n                    if rws <= smat <= rwe:\n                        rsls.append(sl)\n                if rsls:\n                    # เรียงตาม bar_nums (เวลาจริงของ swing low)\n                    rsls.sort(key=lambda s: s['bar_nums'][0])\n                    print(f\"  {'─'*63}\")\n                    print(f\"  {(\"🔵 Swing Low\" if direction==\"up\" else \"🔴 Swing High\")} ที่พบ  ({len(rsls)} จุด)\")\n                    for j, sl in enumerate(rsls, 1):\n                        thick_tag = ' [หนา]' if sl.get('thick') else ''\n                        ref_key = 'sl_b1_time' if direction == 'up' else 'sh_b1_time'\n                        used = any(e.get(ref_key,'') == sl['b1_time'] for e in rent)\n                        used_tag = '  ← entry' if used else ''\n                        pct      = sl.get('zone_pct', 0)\n                        zone     = sl.get('in_zone', False)\n                        zone_tag = ' 🎯' if zone else ''\n                        if direction == 'up':\n                            price_str = f\"body_lo={sl['body_lo']:.3f}\"\n                        else:\n                            price_str = f\"body_hi={sl['body_hi']:.3f}\"\n                        print(f\"  {j:>3}.  {sl['b1_time']} – {sl['b2_time']}  \"\n                              f\"{price_str}  \"\n                              f\"{pct:5.1f}%{zone_tag}{thick_tag}{used_tag}\")\n\n            if rent:\n                print(f\"  {'─'*63}\")\n                print(f\"  📍 Entry ในช่วงนี้  ({len(rent)} จุด)\")\n                for j, e in enumerate(rent, 1):\n                    ref = f\"(REF: {e['sl_b1_time'][6:]})\" if e.get('sl_b1_time') else \"\"\n                    print(f\"  {j:>3}.  📌 {e['entry_time']}  \"\n                          f\"@ {e['entry_price']:.3f}  {ref}\")\n                print(f\"  {'─'*63}\")\n\n    # ── Summary ────────────────────────────────────────────────\n    lens    = [len(r) for r in runs]\n    best_ri = lens.index(max(lens)) + 1\n    best_h, best_m = divmod(max(lens) * 5, 60)\n\n    # ตรวจ overlap หลัง grouping (ควรเป็น 0)\n    overlaps = sum(\n        1 for i in range(1, len(runs))\n        if runs[i][0]['ws'] <= runs[i-1][-1]['we']\n    )\n\n    print(\"\\n\" + DBL)\n    print(\"  สรุป\")\n    print(\"  \" + SEP)\n    print(f\"  ช่วงเทรน{'ขึ้น' if direction=='up' else 'ลง'}ทั้งหมด: {len(runs)} ช่วง\")\n    print(f\"  Windows ที่แสดง   : {total_shown:,}  (ข้ามไป {skipped:,})\")\n    print(f\"  ช่วงยาวสุด        : ช่วงที่ {best_ri} ({max(lens)} windows = {best_h}ชม. {best_m:02d}นาที)\")\n    print(f\"  เวลาแรก           : {runs[0][0]['ws'].strftime('%d/%m/%Y %H:%M')} — {runs[0][0]['we'].strftime('%d/%m/%Y %H:%M')}\")\n    print(f\"  เวลาสุดท้าย       : {runs[-1][-1]['ws'].strftime('%d/%m/%Y %H:%M')} — {runs[-1][-1]['we'].strftime('%d/%m/%Y %H:%M')}\")\n    if overlaps == 0:\n        print(\"  ✅ ช่วงไม่คาบเกี่ยวกัน\")\n    else:\n        print(f\"  ⚠️  พบช่วงคาบเกี่ยว: {overlaps} คู่\")\n    print(DBL)\n\n\n# ── Entry Scanner Functions ──────────────────────────────────\n\n# ── swing_v414 (inlined) ────────────────────────────────────\n\ndef bsize(b): return (max(b[2],b[5]) - min(b[2],b[5])) * 100\ndef bhi(b):   return max(b[2], b[5])\ndef blo(b):   return min(b[2], b[5])\ndef O(b):     return b[2]\ndef H(b):     return b[3]\ndef L(b):     return b[4]\ndef C(b):     return b[5]\n\ndef _check_high(bars, li, rj, mid, R55, thick_exc=False, right2_exc=False):\n    left_range  = [li-1] if thick_exc else [li-3, li-2, li-1]\n    right_range = [rj+1] if thick_exc else [rj+1, rj+2]\n    for k in left_range:\n        if k < 0: continue\n        b = bars[k]\n        if O(b) >= mid or C(b) >= mid: return False\n    for k in right_range:\n        if k >= len(bars):\n            return right2_exc\n        b = bars[k]\n        if O(b) >= mid or C(b) >= mid: return False\n    return True\n\ndef _check_low(bars, li, rj, mid, R55, thick_exc=False, right2_exc=False):\n    left_range  = [li-1] if thick_exc else [li-3, li-2, li-1]\n    right_range = [rj+1] if thick_exc else [rj+1, rj+2]\n    for k in left_range:\n        if k < 0: continue\n        b = bars[k]\n        if O(b) <= mid or C(b) <= mid: return False\n    for k in right_range:\n        if k >= len(bars):\n            return right2_exc\n        b = bars[k]\n        if O(b) <= mid or C(b) <= mid: return False\n    return True\n\ndef scan_swings(bars, R55, cur_H=None, hl_zone_hi=None,\n                cur_L=None, lh_zone_lo=None):\n    thresh1 = R55 * 0.01\n    thresh5 = R55 * 0.10\n    highs, lows = [], []\n    seen_h, seen_l = set(), set()\n\n    def try_sh(li, rj, c1, c3):\n        key = (c1[0], c3[0])\n        if key in seen_h: return\n        if abs(C(c1) - O(c3)) * 100 > pair_thresh: return\n        if bsize(c1) < thresh1 or bsize(c3) < thresh1: return\n        mid = (C(c1) + O(c3)) / 2\n        thick = bsize(c1) > thresh5 or bsize(c3) > thresh5\n        r2_exc = bool(cur_L is not None and hl_zone_hi is not None\n                      and cur_L <= hl_zone_hi)\n        if _check_high(bars, li, rj, mid, R55,\n                       thick_exc=thick, right2_exc=r2_exc):\n            seen_h.add(key)\n            highs.append({\"body_hi\": max(bhi(c1), bhi(c3)), \"mid\": mid,\n                          \"times\": [bars[li][1], bars[rj][1]],\n                          \"bar_nums\": [bars[li][0], bars[rj][0]],\n                          \"thick\": thick})\n\n    def try_sl(li, rj, c1, c3):\n        key = (c1[0], c3[0])\n        if key in seen_l: return\n        if abs(C(c1) - O(c3)) * 100 > pair_thresh: return\n        if bsize(c1) < thresh1 or bsize(c3) < thresh1: return\n        mid = (C(c1) + O(c3)) / 2\n        thick = bsize(c1) > thresh5 or bsize(c3) > thresh5\n        r2_exc = bool(cur_H is not None and lh_zone_lo is not None\n                      and cur_H >= lh_zone_lo)\n        if _check_low(bars, li, rj, mid, R55,\n                      thick_exc=thick, right2_exc=r2_exc):\n            seen_l.add(key)\n            lows.append({\"body_lo\": min(blo(c1), blo(c3)), \"mid\": mid,\n                         \"times\": [bars[li][1], bars[rj][1]],\n                         \"bar_nums\": [bars[li][0], bars[rj][0]],\n                         \"thick\": thick})\n\n    for i in range(len(bars) - 1):\n        j = i + 1\n        bi, bj = bars[i], bars[j]\n        pair_thresh = max(R55 * 0.01, 100)\n        if abs(C(bi) - O(bj)) * 100 > pair_thresh: continue\n        bid = bsize(bi) < thresh1\n        bjd = bsize(bj) < thresh1\n        if not bid and not bjd:\n            try_sh(i, j, bi, bj); try_sl(i, j, bi, bj)\n        elif not bid and bjd:\n            if j + 1 < len(bars):\n                try_sh(i, j+1, bi, bars[j+1])\n                try_sl(i, j+1, bi, bars[j+1])\n        elif bid and not bjd:\n            if i - 1 >= 0:\n                try_sh(i-1, j, bars[i-1], bj)\n                try_sl(i-1, j, bars[i-1], bj)\n    return highs, lows\n\n# ────────────────────────────────────────────────────────────\n\n\n# ─── Bar-by-Bar Entry Scanner ────────────────────────────────\ndef detect_down_father(bars: list, i: int, R55: float,\n                       max_bars: int = 8,\n                       min_pct: float = 40.0,\n                       max_pct: float = 100.0) -> bool:\n    \"\"\"\n    ตรวจว่า bar ปัจจุบัน (index i) เป็นจุดสิ้นสุดของ แท่งพ่อลง ไหม\n    เงื่อนไข:\n      - 1-8 แท่ง (สีเดียวกัน หรือสลับสีก็ได้)\n      - Net movement: Open แท่งแรก → Close แท่งสุดท้าย เป็นขาลง\n      - Body รวม = 40-100% ของ R55\n    \"\"\"\n    if R55 <= 0:\n        return False\n    cur_close = bars[i][5]\n    for n in range(1, max_bars + 1):\n        start = i - n + 1\n        if start < 0:\n            break\n        first_open  = bars[start][2]\n        total_body  = (first_open - cur_close) * 100  # pip (ลงถ้า > 0)\n        pct         = total_body / R55 * 100\n        if min_pct <= pct <= max_pct:\n            return True\n    return False\n\n\ndef scan_entries_bar_by_bar(df: pd.DataFrame) -> tuple:\n    \"\"\"\n    จำลองการเทรดทีละแท่ง:\n      1. เลื่อน window 55 แท่งทีละ 1 แท่ง\n      2. ถ้า window อยู่ใน merged trend (จาก trend_results) → หา Swing Low\n         ถ้าไม่มี trend_results → fallback ตรวจ C1-C6 เอง\n      3. ทุกแท่ง: เช็ค Low ≤ level ใน watch_list → บันทึก entry\n    \"\"\"\n    from IPython.display import clear_output\n\n    n         = len(df)\n    total_w   = n - WINDOW + 1\n    entries           = []\n    all_swing_lows    = []\n    last_trend_we     = None   # we ล่าสุดที่ผ่าน criteria (สำหรับ ws-overlap)\n    last_confirmed_we = None   # we ล่าสุดที่ผ่านจริงๆ (สำหรับ retroactive cleanup)\n    sl_key_times      = {}     # key → marked_at เพื่อ cleanup seen_sl_keys\n    segment_start_we  = None   # we เริ่มต้นของ merged segment ปัจจุบัน   # เก็บทุก swing low ที่พบ (สำหรับ display)\n    watch_list      = {}   # key=body_lo : {'sl': sl_info, 'marked_at': time}\n    seen_sl_keys    = set()   # ป้องกัน mark Swing Low ซ้ำ\n\n\n    # แปลง df → bars format\n    bars = [\n        (int(i + 1),\n         row['time'].strftime('%Y-%m-%d %H:%M'),\n         row['open'], row['high'], row['low'], row['close'])\n        for i, row in df.iterrows()\n    ]\n\n    UPDATE_EVERY = max(1, min(200, total_w // 100))\n\n    def _bar(d, t, w=28):\n        f = int(w * d / t)\n        return '█' * f + '░' * (w - f)\n\n    def _show(done, cur_t, cur_is_trend):\n        pct = done / total_w * 100\n        clear_output(wait=True)\n        print('═' * 64)\n        print('  XAUUSD Entry Scanner — Bar-by-Bar')\n        print('═' * 64)\n        print(f\"  [{_bar(done, total_w)}] {pct:5.1f}%\")\n        print(f\"  สแกนแล้ว : {done:,} / {total_w:,} windows\")\n        print()\n        print(f\"  Window ปัจจุบัน : {cur_t}\")\n        trend_icon = '🟢 เป็นเทรน' if cur_is_trend else '⬜ ไม่ใช่เทรน'\n        print(f\"  สถานะ          : {trend_icon}\")\n        print(f\"  กำลังจับตา     : {len(watch_list)} ระดับ\")\n        print('─' * 64)\n        print(f\"  พบ entry : {len(entries)} จุด\")\n        if entries:\n            last = entries[-1]\n            print(f\"  ล่าสุด   : {last['entry_time']}  \"\n                  f\"@ {last['entry_price']:.3f}  \"\n                  f\"(REF: {last['sl_b1_time']})\")\n        print('═' * 64)\n\n    # ── R55 อ้างอิงคงที่จาก 55 แท่งแรก ─────────────────────\n    R55_ref = (max(b[3] for b in bars[:55]) -\n               min(b[4] for b in bars[:55])) * 100\n    thresh_body = R55_ref * SL_MIN_BODY_PCT\n\n    # ── Main loop ────────────────────────────────────────────\n    for i in range(WINDOW, n + 1):\n        w    = df.iloc[i - WINDOW : i]\n        H55  = w['high'].max()\n        L55  = w['low'].min()\n        R55  = H55 - L55\n        done = i - WINDOW + 1\n        cur_bar = bars[i - 1]\n        cur_time_str = f\"{w['time'].iloc[0].strftime('%d/%m %H:%M')} → \" \\\n                       f\"{w['time'].iloc[-1].strftime('%d/%m %H:%M')}\"\n\n        is_trend = False\n        if R55 >= MIN_R55:\n            lb_end   = i - WINDOW\n            lb_start = max(0, lb_end - LOOKBACK)\n            lb = df.iloc[lb_start:lb_end] if lb_end > lb_start else None\n\n            # ── Incremental ws-overlap: เทียบกับ last_trend_we ──\n            p1 = check_c1(w, L55, R55)[1]\n            p2 = check_c2(w, L55, R55)[1]\n            p3 = check_c3(w, R55)[1]\n            p4 = check_c4(w, R55)[1]\n            p5 = check_c5(lb, R55)[1]\n            p6 = check_c6(w, lb, L55, R55)[1]\n            passed = p1 and p2 and p3 and p4 and p5 and p6\n\n            cur_we = w['time'].iloc[-1]\n            cur_ws = w['time'].iloc[0]\n\n            # ── ตรวจ แท่งพ่อลง (anti-trend filter) ──────────────\n            if is_trend:\n                H55 = max(b[3] for b in w_bars)\n                L55_chk = min(b[4] for b in w_bars)\n                R55_chk = (H55 - L55_chk) * 100\n                if R55_chk > 0 and detect_down_father(bars, i-1, R55_chk):\n                    # พบแท่งพ่อลง → ยกเลิกเทรน + cleanup\n                    is_trend      = False\n                    last_trend_we  = None\n                    last_confirmed_we = None\n                    # เคลียร์ watch_list และ entries ที่ยังไม่ fire\n                    watch_list.clear()\n\n            if passed:\n                # ตรวจว่า segment ใหม่ต่อเนื่องกับของเก่าไหม\n                if last_confirmed_we is not None and cur_ws >= last_confirmed_we:\n                    # ❌ ไม่ต่อเนื่อง → retroactive cleanup\n                    # ลบ entries ที่เกิดใน gap period ออก\n                    entries[:] = [e for e in entries\n                                  if e['marked_at'] <= last_confirmed_we]\n                    all_swing_lows[:] = [s for s in all_swing_lows\n                                         if s['marked_at'] <= last_confirmed_we]\n                    # ลบ watch_list levels ที่ add ใน gap period\n                    for _k in [k for k, v in watch_list.items()\n                               if v['marked_at'] > last_confirmed_we]:\n                        watch_list.pop(_k)\n                    # ลบ seen_sl_keys ที่ add ใน gap period ด้วย\n                    for _k in [k for k, t in sl_key_times.items()\n                               if t > last_confirmed_we]:\n                        seen_sl_keys.discard(_k)\n                        sl_key_times.pop(_k, None)\n                    segment_start_we  = None   # reset segment\n                is_trend      = True\n                last_trend_we  = cur_we\n                # ตรวจว่า segment ใหม่หรือต่อเนื่อง ก่อน update last_confirmed_we\n                _prev_confirmed = last_confirmed_we\n                last_confirmed_we = cur_we\n                if _prev_confirmed is None or cur_ws >= _prev_confirmed:\n                    # segment ใหม่ → reset segment_start_we\n                    segment_start_we = cur_we\n                # else: ต่อเนื่อง → ไม่เปลี่ยน segment_start_we\n            elif last_trend_we is not None and cur_ws < last_trend_we:\n                # ws คร่อม we ก่อน → ยังอยู่ใน merged trend\n                is_trend = True\n            else:\n                is_trend = False\n\n        # ── ถ้าเป็นเทรน: mark Swing Low ใหม่ ────────────────\n        if is_trend:\n            w_bars = bars[i - WINDOW : i]\n            _, lows = scan_swings(w_bars, R55_ref)\n\n            for sl in lows:\n                min_body = min(bsize(w_bars[sl['bar_nums'][0] - w_bars[0][0]]),\n                               bsize(w_bars[sl['bar_nums'][1] - w_bars[0][0]]))\n                if min_body <= thresh_body:\n                    continue\n\n                # key = tuple ของ bar_nums (ไม่ซ้ำ)\n                key = tuple(sl['bar_nums'])\n                if key in seen_sl_keys:\n                    continue\n\n                seen_sl_keys.add(key)\n                sl_key_times[key] = w['time'].iloc[-1]  # track เวลาที่ add\n                level = sl['body_lo']\n                _pct = (level - L55) / (R55 / 100) if R55 > 0 else 0\n                all_swing_lows.append({\n                    'body_lo'    : level,\n                    'b1_time'    : sl['times'][0][5:16],\n                    'b2_time'    : sl['times'][1][5:16],\n                    'bar_nums'   : sl['bar_nums'],\n                    'thick'      : sl.get('thick', False),\n                    'marked_at'  : w['time'].iloc[-1],\n                    'zone_pct'   : _pct,\n                    'in_zone'    : 65.0 <= _pct <= 85.0,\n                })\n                # ── Zone filter: entry REF ต้องอยู่ใน 65%-85% ของ R55 ──\n                zone_lo = L55 + R55 * 0.65\n                zone_hi = L55 + R55 * 0.85\n                if not (zone_lo <= level <= zone_hi):\n                    continue  # แสดงใน display แต่ไม่ใช้เป็น entry REF\n\n                if level not in watch_list:\n                    watch_list[level] = {\n                        'sl'          : sl,\n                        'sl_b1_time'  : sl['times'][0][5:16],\n                        'sl_b2_time'  : sl['times'][1][11:16],\n                        'marked_at'   : w['time'].iloc[-1],\n                    }\n\n        # ── เช็ค entry: Low ≤ level ใน watch_list ────────────\n        # เข้าได้เฉพาะตอนที่ยังอยู่ใน confirmed trend\n        # (cur_bar time <= last_confirmed_we ไม่ใช่แค่ overlap)\n        _cur_time = w['time'].iloc[-1] if i <= len(df) else None\n        _in_conf  = (last_confirmed_we is not None and\n                     _cur_time is not None and\n                     _cur_time <= last_confirmed_we)\n        if not _in_conf:\n            cur_low = None\n        else:\n            cur_low = cur_bar[4]\n        win_start_barnum = bars[i - WINDOW][0]  # bar_num แรกของ window ปัจจุบัน\n        for level in (list(watch_list.keys()) if cur_low is not None else []):\n            info = watch_list[level]\n            sl_bar_nums = info['sl']['bar_nums']\n            # ถ้า swing low หลุดออกจาก window ปัจจุบัน → ลบออก ห้ามใช้\n            if max(sl_bar_nums) < win_start_barnum:\n                watch_list.pop(level)\n                continue\n            if cur_low <= level:\n                watch_list.pop(level)\n                entries.append({\n                    'entry_time'  : cur_bar[1][5:16],\n                    'entry_price' : level,\n                    'entry_low'   : cur_low,\n                    'sl_b1_time'  : info['sl_b1_time'],\n                    'sl_b2_time'  : info['sl_b2_time'],\n                    'marked_at'   : info['marked_at'],\n                    'entry_bar_i' : i - 1,\n                    'segment_id'  : segment_start_we,\n                })\n\n        # progress\n        if done % UPDATE_EVERY == 0 or done == total_w or entries and \\\n                entries[-1]['entry_time'] == cur_bar[1][5:16]:\n            _show(done, cur_time_str, is_trend)\n\n    # final\n    print('═' * 64)\n    print('  ✅ Entry scan เสร็จสิ้น')\n    print('═' * 64)\n    print(f\"  พบ entry : {len(entries)} จุด\")\n    print('═' * 64)\n    return entries, all_swing_lows\n\n\n# ─── Display Entry Results ────────────────────────────────────\ndef display_entries(entries: list) -> None:\n    SEP = '─' * 63\n    DBL = '═' * 63\n    if not entries:\n        print(\"\\n  ❌ ไม่พบ entry\")\n        return\n    print('\\n' + DBL)\n    print(f\"  ผลการสแกน Entry Points  ({len(entries)} จุด)\")\n    print(DBL)\n    print(f\"  {'#':>3}  {'เวลาเข้า':<18}  {'Entry Price':>12}  อ้างอิง\")\n    print('  ' + SEP)\n    for i, e in enumerate(entries, 1):\n        ref = (f\"(REF: Swing Low {e['sl_b1_time'][6:]})\"\n                  if direction == 'up' else\n                  f\"(REF: Swing High {e.get('sh_b1_time','')[6:]})\")\n        print(f\"  {i:>3}.  {e['entry_time']:<18}  \"\n              f\"{e['entry_price']:>12.3f}  {ref}\")\n    print('\\n' + DBL)\n\ndef display_trades(trades_up: list = None, trades_down: list = None):\n    \"\"\"แสดงตารางเทรดรวม uptrend + downtrend พร้อมคอลัม BUY/SELL\"\"\"\n    # รวม trades ทั้งสองทิศ พร้อมใส่ direction\n    all_trades = []\n    for t in (trades_up or []):\n        all_trades.append({**t, 'direction': 'BUY'})\n    for t in (trades_down or []):\n        all_trades.append({**t, 'direction': 'SELL'})\n    # เรียงตาม entry_time\n    all_trades.sort(key=lambda t: t['entry_time'])\n\n    SEP = '─' * 108\n    DBL = '═' * 108\n    print(f'\\n{DBL}')\n    print(f'  📊 สรุปผลการเทรดรวม  ({len(all_trades)} รายการ)')\n    print(f'  {SEP}')\n    print(f\"  {'':3} {'Dir':<5} {'เข้า':<14} {'ออก':<14} {'REF':<14} {'Entry':>10} \"\n          f\"{'TP':>10} {'SL':>10} {'R:R':>5} {'Lot':>7} {'pip':>8} {'USD':>8}\")\n    print(f'  {SEP}')\n    wins = losses = 0\n    net_pip = net_usd = 0.0\n    for t in all_trades:\n        if t['result'] == 'WIN':\n            icon = '✅'; wins += 1\n        elif t['result'] == 'LOSS':\n            icon = '❌'; losses += 1\n        else:\n            icon = '🔵'\n        d_icon = 'UP  ' if t['direction'] == 'BUY' else 'DOWN'\n        if t['result'] != 'OPEN':\n            net_pip += t['pip']\n            net_usd += t.get('usd', 0)\n        print(f\"  {icon}  {d_icon}  {t['entry_time']:<14} {t['close_time']:<14}\"\n              f\" {t.get('ref',''):<14} {t['entry']:>10.3f} {t['tp']:>10.3f} {t['sl']:>10.3f}\"\n              f\" {t.get('rr',0):>5.2f} {t.get('lot',0):>7.4f} {t['pip']:>+8.1f} {t.get('usd',0):>+8.2f}\")\n    total = wins + losses\n    wr    = wins / total * 100 if total else 0\n    print(f'  {SEP}')\n    # แยกสรุป uptrend / downtrend\n    up_t   = [t for t in all_trades if t['direction'] == 'BUY']\n    dn_t   = [t for t in all_trades if t['direction'] == 'SELL']\n    def _stat(lst):\n        w = sum(1 for t in lst if t['result']=='WIN')\n        l = sum(1 for t in lst if t['result']=='LOSS')\n        n = w + l\n        wr_ = w/n*100 if n else 0\n        net_ = sum(t.get('usd',0) for t in lst if t['result']!='OPEN')\n        return w, l, n, wr_, net_\n    uw,ul,un,uwr,unet = _stat(up_t)\n    dw,dl,dn,dwr,dnet = _stat(dn_t)\n    print(f'  📈 Uptrend  (BUY)  : {un} orders  W{uw}/L{ul}  '\n          f'WR={uwr:.0f}%  ({unet:+.2f} USD)')\n    print(f'  📉 Downtrend(SELL) : {dn} orders  W{dw}/L{dl}  '\n          f'WR={dwr:.0f}%  ({dnet:+.2f} USD)')\n    print(f'  {SEP}')\n    print(f'  รวม  Win {wins}/{total}  '\n          f'WR={wr:.0f}%  '\n          f'Net={net_pip:+.1f}pip  '\n          f'({net_usd:+.2f} USD)  '\n          f\"{'✅' if net_pip > 0 else ('❌' if net_pip < 0 else '➖')}\")\n    print(f'{DBL}')\n\nprint('✅ โหลด functions ทั้งหมดสำเร็จ')\n"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "id": "cell_run",
   "metadata": {},
   "outputs": [],
   "source": [
    "# ============================================================\n# Cell 4 — ⚙️ Config + รัน\n# ============================================================\n\n# ── ตั้งค่าที่นี่ ───────────────────────────────────────────\nCSV_PATH = '/content/drive/MyDrive/XAUUSD_M5-Backtest 2026.csv'\n\nWINDOW        = 55    # จำนวนแท่งใน window (อย่าแก้ถ้าไม่จำเป็น)\nLOOKBACK      = 25    # จำนวนแท่ง lookback สำหรับ C5\nTIMEFRAME_MIN = 5     # Timeframe เป็นนาที (M5=5, M15=15, H1=60)\n\n# ── Criteria Thresholds ────────────────────────────────────\nC1_MIN        = 0.60  # C1: Close >= 60% R55\nC2_MIN        = 0.55  # C2: Recent low (5 bars) >= 55% R55\nC3_MAX        = 0.30  # C3: Drop from HH <= 30% R55\nC4_MIN        = 0.25  # C4: Impulse >= 25% R55\nC5_MAX        = 0.30  # C5: No bearish bar > 30% R55 in lookback\nC6_MAX_START  = 0.40  # C6: ราคาต้น window <= 40% R55\nC6_START_BARS = 5     # C6: ตรวจ N แท่งแรกของ window\n\nMIN_R55       = 3.0   # ข้ามถ้า R55 < 3 USD\nPORTFOLIO     = 1000.0  # เงินทุน USD (ใช้คำนวณ Lot = 10% / SL_pip)\nSL_MIN_BODY_PCT = 0.04  # Entry: Swing Low min body = 4% R55\nVERBOSE       = True  # แสดง criteria detail แท่งแรก/สุดท้าย\nVERSION       = 'v1.4'  # version สำหรับชื่อ Sheet\nEXPORT_GSHEET = True    # True = สร้าง Google Sheet อัตโนมัติ\n\n# ── โหลดข้อมูล ─────────────────────────────────────────────\nprint('=' * 65)\nprint('  XAUUSD Uptrend + Entry Scanner')\nprint('=' * 65)\nprint(f'  ไฟล์ : {CSV_PATH}')\n\ndf = load_data(CSV_PATH)\n\nprint(f'  แท่ง : {len(df):,}')\nprint(f'  เวลา : {df[\"time\"].iloc[0].strftime(\"%d/%m/%Y %H:%M\")} → '\n      f'{df[\"time\"].iloc[-1].strftime(\"%d/%m/%Y %H:%M\")}')\nprint()\n\n# ── 1. สแกนและ Simulate ทั้งหมดก่อน (มี progress bar) ──────\nresults       = scan_uptrend(df)\nentries, swing_lows = scan_entries_bar_by_bar(df)\ntrades_up     = simulate_trades(entries, df, portfolio=PORTFOLIO)\n\nresults_down  = scan_downtrend(df)\nentries_down, swing_highs = scan_entries_bar_by_bar_down(df)\ntrades_down   = simulate_trades_down(entries_down, df, portfolio=PORTFOLIO)\n\n# ── 2. แสดงผลทั้งหมดหลังสแกนเสร็จ (ไม่มี clear_output แล้ว) ─\ndisplay_results(results, entries=entries, swing_lows=swing_lows)\ndisplay_results(results_down, entries=entries_down,\n                swing_lows=swing_highs, direction='down')\n\n# ── 3. สรุปรวม ───────────────────────────────────────\ndisplay_trades(trades_up=trades_up, trades_down=trades_down)\n\n"
   ]
  },
  {
   "id": "cell_export",
   "cell_type": "code",
   "source": [
    "# ============================================================\n# Cell 5 — Export Google Sheet\n# ============================================================\n# ตั้งค่าก่อน Export\nVERSION       = 'v3.3'   # version สำหรับชื่อ Sheet\nEXPORT_GSHEET = True     # True = สร้าง Google Sheet\n\n# ════════════════════════════════════════════════════════════════\n# GOOGLE SHEETS EXPORT\n# ════════════════════════════════════════════════════════════════\n\ndef export_to_gsheet(trades_up: list, trades_down: list,\n                     csv_filename: str, version: str = \"v1.4\"):\n    \"\"\"\n    Export ผล Backtest Uptrend+Downtrend Scanner → Google Sheet\n    9 Tab: รายการเทรด / สรุปรวม / แยก UP-DOWN / รายวัน /\n           รายเดือน / รายวันสัปดาห์ / Session / เงื่อนไข / Equity Curve\n    \"\"\"\n    import re, os\n    from collections import defaultdict\n    from datetime import datetime\n\n    try:\n        from googleapiclient.discovery import build\n    except ImportError:\n        print(\"❌ ต้องติดตั้ง: !pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib\")\n        return None\n\n    # ใช้ credentials จาก cell_mount (_GSHEET_CREDS)\n    try:\n        creds = _GSHEET_CREDS\n    except NameError:\n        print(\"❌ ยังไม่ได้รัน Cell 2 (Mount + Auth) — กรุณารัน Cell 2 ก่อน\")\n        return None\n\n    sheets = build('sheets', 'v4', credentials=creds)\n    drive  = build('drive',  'v3', credentials=creds)\n\n    # รวม trades ทั้งสอง + ใส่ direction\n    all_trades = []\n    for t in (trades_up or []):\n        all_trades.append({**t, 'direction': 'UP'})\n    for t in (trades_down or []):\n        all_trades.append({**t, 'direction': 'DOWN'})\n    all_trades.sort(key=lambda t: t['entry_time'])\n\n    init_port = PORTFOLIO\n    base        = re.sub(r'\\.csv$', '', os.path.basename(csv_filename), flags=re.IGNORECASE)\n    sheet_title = f\"{base} updown scan {version}\"\n    ss  = sheets.spreadsheets().create(\n              body={'properties':{'title':sheet_title}},\n              fields='spreadsheetId,spreadsheetUrl').execute()\n    sid = ss['spreadsheetId']\n    url = ss['spreadsheetUrl']\n    print(f\"✅ สร้าง Google Sheet: {sheet_title}\")\n    print(f\"   🔗 {url}\")\n\n    WEEKDAY_TH = {0:'จันทร์',1:'อังคาร',2:'พุธ',3:'พฤหัส',4:'ศุกร์',5:'เสาร์',6:'อาทิตย์'}\n    SESS_TIME  = {\n        'Asian':'00:00–07:00','Asian+London':'07:00–09:00',\n        'London':'09:00–13:00','London+NewYork':'13:00–16:00',\n        'New York':'16:00–22:00','After NY':'22:00–24:00',\n    }\n\n    # ดึงปีจาก data จริง เพื่อไม่ hardcode\n    _first_time = all_trades[0].get('entry_time','') if all_trades else ''\n    _data_year = int(_first_time[:4]) if len(_first_time) >= 4 and _first_time[:4].isdigit() else 2026\n\n    def parse_dt(s):\n        \"\"\"แปลง entry_time 'MM-DD HH:MM' หรือ 'YYYY-MM-DD HH:MM' เป็น datetime\"\"\"\n        try:\n            if len(s) == 11:  # MM-DD HH:MM — ใช้ปีจาก data จริง\n                return datetime.strptime(f\"{_data_year}-{s}\", '%Y-%m-%d %H:%M')\n            return datetime.strptime(s[:16], '%Y-%m-%d %H:%M')\n        except: return None\n\n    def get_date(t):\n        dt = parse_dt(t.get('entry_time',''))\n        return dt.strftime('%Y-%m-%d') if dt else ''\n\n    def get_sess(t):\n        dt = parse_dt(t.get('entry_time',''))\n        if not dt: return 'Other'\n        h = dt.hour\n        if  0<=h< 7: return 'Asian'\n        if  7<=h< 9: return 'Asian+London'\n        if  9<=h<13: return 'London'\n        if 13<=h<16: return 'London+NewYork'\n        if 16<=h<22: return 'New York'\n        return 'After NY'\n\n    def get_wd(t):\n        dt = parse_dt(t.get('entry_time',''))\n        return dt.weekday() if dt else None\n\n    def stat_group(lst):\n        w = sum(1 for t in lst if t['result']=='WIN')\n        l = sum(1 for t in lst if t['result']=='LOSS')\n        n = w+l; wr = w/n*100 if n else 0\n        net_pip = sum(t['pip'] for t in lst if t['result']!='OPEN')\n        net_usd = sum(t.get('usd',0) for t in lst if t['result']!='OPEN')\n        return w, l, n, wr, net_pip, net_usd\n\n    # ── Sheet 1: รายการเทรด ─────────────────────────────────────\n    hdr1 = [['#','วันที่','เวลาเปิด','เวลาปิด','DIR','REF',\n              'Entry','TP','SL','R:R','Lot','pip','USD','ผล','Session','Segment']]\n    rows1 = []\n    for i, t in enumerate(all_trades, 1):\n        dt = parse_dt(t.get('entry_time',''))\n        rows1.append([\n            i,\n            dt.strftime('%Y-%m-%d') if dt else '',\n            t.get('entry_time','')[-5:] if len(t.get('entry_time',''))>=5 else '',\n            t.get('close_time',''),\n            t['direction'],\n            t.get('ref',''),\n            t.get('entry',0),\n            t.get('tp',0),\n            t.get('sl',0),\n            t.get('rr',1.0),\n            round(t.get('lot',0),4),\n            round(t.get('pip',0),1),\n            round(t.get('usd',0),2),\n            t['result'],\n            get_sess(t),\n            str(t.get('segment_id',''))[:16] if t.get('segment_id') else '',\n        ])\n\n    dg = defaultdict(lambda:{'w':0,'l':0,'pip':0.,'usd':0.})\n    _eq_run = init_port\n    for t in all_trades:\n        d = get_date(t)\n        if t['result']=='WIN':   dg[d]['w']+=1\n        elif t['result']=='LOSS': dg[d]['l']+=1\n        if t['result']!='OPEN':\n            dg[d]['pip']+=t['pip']\n            dg[d]['usd']+=t.get('usd',0)\n            _eq_run += t.get('usd',0)\n        dg[d]['eq_end'] = _eq_run\n\n    # นับวันชนะ/แพ้ (dg populate แล้ว)\n    total_days = len([d for d in dg if d])\n    win_days   = sum(1 for d,g in dg.items() if g['usd'] > 0)\n    loss_days  = sum(1 for d,g in dg.items() if g['usd'] < 0)\n    even_days  = total_days - win_days - loss_days\n\n    # ── Sheet 2: สรุปรวม ─────────────────────────────────────────\n    w_all,l_all,n_all,wr_all,net_pip_all,net_usd_all = stat_group(all_trades)\n    w_up, l_up, n_up, wr_up, np_up, nu_up   = stat_group([t for t in all_trades if t['direction']=='UP'])\n    w_dn, l_dn, n_dn, wr_dn, np_dn, nu_dn   = stat_group([t for t in all_trades if t['direction']=='DOWN'])\n\n    # ── Streaks + Depth (orders & USD) ─────────────────\n    mws=mls=cws=cls=0\n    mwusd=mlusd=cwusd=clusd=0.0\n    for t in all_trades:\n        u = t.get('usd',0) if t['result']!='OPEN' else 0\n        if t['result']=='WIN':\n            cws+=1; cls=0; mws=max(mws,cws)\n            cwusd+=u; clusd=max(0.0,clusd-u); mwusd=max(mwusd,cwusd)\n        elif t['result']=='LOSS':\n            cls+=1; cws=0; mls=max(mls,cls)\n            clusd+=abs(u); cwusd=max(0.0,cwusd-abs(u)); mlusd=max(mlusd,clusd)\n\n    # ── Drawdown depth + Win depth ─────────────────────\n    # Orders: dd+1 on LOSS / -1 on WIN (min 0), wd+1 on WIN / -1 on LOSS (min 0)\n    dd_ord = wd_ord = max_dd_ord = max_wd_ord = 0\n    for t in all_trades:\n        if t['result'] == 'LOSS':\n            dd_ord += 1;       max_dd_ord = max(max_dd_ord, dd_ord)\n            wd_ord  = max(0, wd_ord - 1)\n        elif t['result'] == 'WIN':\n            wd_ord += 1;       max_wd_ord = max(max_wd_ord, wd_ord)\n            dd_ord  = max(0, dd_ord - 1)\n\n    # USD: สะสม pnl, reset เมื่อฝั่งตรงข้ามข้าม 0\n    dd_usd_run = wd_usd_run = max_dd_usd = max_wd_usd = 0.0\n    for t in all_trades:\n        u = t.get('usd',0) if t['result']!='OPEN' else 0\n        dd_usd_run += u\n        if dd_usd_run >= 0:\n            dd_usd_run = 0.0         # ข้าม 0 ฝั่งบวก → reset drawdown\n        else:\n            max_dd_usd = max(max_dd_usd, abs(dd_usd_run))\n        wd_usd_run += u\n        if wd_usd_run <= 0:\n            wd_usd_run = 0.0         # ข้าม 0 ฝั่งลบ → reset windepth\n        else:\n            max_wd_usd = max(max_wd_usd, wd_usd_run)\n\n    rows2 = [\n        ['สรุปรวม Backtest — Uptrend + Downtrend Scanner','',''],\n        ['Version', version, ''],\n        ['','',''],\n        ['รายการ','ค่า',''],\n        ['เทรดทั้งหมด', n_all, ''],\n        ['ชนะ', w_all, ''],\n        ['แพ้',  l_all, ''],\n        ['Win Rate', f'{wr_all:.1f}%',''],\n        ['','',''],\n        ['Net pip',  round(net_pip_all,1), ''],\n        ['Net USD',  round(net_usd_all,2), ''],\n        ['Portfolio เริ่มต้น', '', round(init_port,2)],\n        ['Portfolio สุดท้าย', '', round(init_port+net_usd_all,2)],\n        ['','',''],\n        ['── วันเทรด ──','',''],\n        ['วันที่มีการเทรดทั้งหมด', total_days, 'วัน'],\n        ['วันที่กำไร (net USD > 0)', win_days,\n         f'{win_days/total_days*100:.0f}%' if total_days else '-'],\n        ['วันที่ขาดทุน (net USD < 0)', loss_days,\n         f'{loss_days/total_days*100:.0f}%' if total_days else '-'],\n        ['วันที่เสมอ', even_days,\n         f'{even_days/total_days*100:.0f}%' if total_days else '-'],\n        ['','',''],\n        ['ชนะติดกันสูงสุด (streak)',  mws,  'ไม้'],\n        ['แพ้ติดกันสูงสุด (streak)',   mls,  'ไม้'],\n        ['Win USD สูงสุด (streak)',    f'+{mwusd:.2f}', 'USD'],\n        ['Loss USD สูงสุด (streak)',   f'-{mlusd:.2f}', 'USD'],\n        ['','',''],\n        ['── Drawdown Depth ──','',''],\n        ['Drawdown depth (orders)', max_dd_ord, 'ไม้'],\n        ['Drawdown depth (USD)',    f'-{max_dd_usd:.2f}', 'USD'],\n        ['','',''],\n        ['── Win Depth ──','',''],\n        ['Win depth (orders)', max_wd_ord, 'ไม้'],\n        ['Win depth (USD)',    f'+{max_wd_usd:.2f}', 'USD'],\n        ['','',''],\n        ['── แยก Direction ──','',''],\n        ['Direction','Orders','WR%', 'Net USD'],\n        ['UP  (Uptrend)',   f'{n_up} orders  W{w_up}/L{l_up}', f'{wr_up:.0f}%', round(nu_up,2)],\n        ['DOWN (Downtrend)',f'{n_dn} orders  W{w_dn}/L{l_dn}', f'{wr_dn:.0f}%', round(nu_dn,2)],\n    ]\n\n    # ── Sheet 3: แยก UP/DOWN ─────────────────────────────────────\n    hdr3 = [['Direction','ชนะ','แพ้','WR%','Net pip','Net USD']]\n    rows3 = []\n    for dlbl, dval in [('UP (Uptrend)','UP'),('DOWN (Downtrend)','DOWN')]:\n        g = [t for t in all_trades if t['direction']==dval]\n        w,l,n,wr,np,nu = stat_group(g)\n        rows3.append([dlbl, w, l, f'{wr:.1f}%', round(np,1), round(nu,2)])\n\n    # ── Sheet 4: รายวัน ─────────────────────────────────────────\n    hdr4 = [['วันที่','ชนะ','แพ้','WR%','Net pip','Net USD','Portfolio (USD)']]\n    rows4 = [[d, g['w'], g['l'],\n              f'{g[\"w\"]/(g[\"w\"]+g[\"l\"])*100:.0f}%' if g[\"w\"]+g[\"l\"] else '-',\n              round(g['pip'],1), round(g['usd'],2),\n              round(g.get('eq_end', init_port), 2)]\n             for d,g in sorted(dg.items())]\n\n\n    # ── Sheet 5: รายเดือน ────────────────────────────────────────\n    mg = defaultdict(lambda:{'w':0,'l':0,'days':set(),'pip':0.,'usd':0.})\n    for t in all_trades:\n        d=get_date(t); ym=d[:7]\n        if t['result']=='WIN':   mg[ym]['w']+=1\n        elif t['result']=='LOSS': mg[ym]['l']+=1\n        if t['result']!='OPEN': mg[ym]['pip']+=t['pip']; mg[ym]['usd']+=t.get('usd',0)\n        mg[ym]['days'].add(d)\n    hdr5 = [['เดือน','วันที่มีเทรด','ชนะ','แพ้','WR%','Net pip','Net USD']]\n    rows5 = [[ym, len(g['days']), g['w'], g['l'],\n              f'{g[\"w\"]/(g[\"w\"]+g[\"l\"])*100:.0f}%' if g[\"w\"]+g[\"l\"] else '-',\n              round(g['pip'],1), round(g['usd'],2)]\n             for ym,g in sorted(mg.items())]\n\n    # ── Sheet 6: รายวันสัปดาห์ ──────────────────────────────────\n    wg = defaultdict(lambda:{'w':0,'l':0,'pip':0.,'usd':0.})\n    for t in all_trades:\n        wd=get_wd(t)\n        if wd is None: continue\n        if t['result']=='WIN':   wg[wd]['w']+=1\n        elif t['result']=='LOSS': wg[wd]['l']+=1\n        if t['result']!='OPEN': wg[wd]['pip']+=t['pip']; wg[wd]['usd']+=t.get('usd',0)\n    hdr6 = [['วัน','ชนะ','แพ้','WR%','Net pip','Net USD']]\n    rows6 = [[WEEKDAY_TH.get(wd,'?'), g['w'], g['l'],\n              f'{g[\"w\"]/(g[\"w\"]+g[\"l\"])*100:.0f}%' if g[\"w\"]+g[\"l\"] else '-',\n              round(g['pip'],1), round(g['usd'],2)]\n             for wd,g in sorted(wg.items())]\n\n    # ── Sheet 7: Session ─────────────────────────────────────────\n    sg = defaultdict(lambda:{'w':0,'l':0,'pip':0.,'usd':0.})\n    for t in all_trades:\n        s=get_sess(t)\n        if t['result']=='WIN':   sg[s]['w']+=1\n        elif t['result']=='LOSS': sg[s]['l']+=1\n        if t['result']!='OPEN': sg[s]['pip']+=t['pip']; sg[s]['usd']+=t.get('usd',0)\n    SESS_ORDER=['Asian','Asian+London','London','London+NewYork','New York','After NY','Other']\n    hdr7 = [['Session','เวลา UTC','ชนะ','แพ้','WR%','Net pip','Net USD']]\n    rows7 = [[s, SESS_TIME.get(s,'—'), g['w'], g['l'],\n              f'{g[\"w\"]/(g[\"w\"]+g[\"l\"])*100:.0f}%' if g[\"w\"]+g[\"l\"] else '-',\n              round(g['pip'],1), round(g['usd'],2)]\n             for s in SESS_ORDER for g in [sg.get(s)] if g]\n\n    # ── Sheet 8: เงื่อนไข ────────────────────────────────────────\n    rows_cond = [\n        ['เงื่อนไขการสแกน — Uptrend + Downtrend Scanner',''],\n        ['Version', version],\n        ['ไฟล์ CSV', csv_filename],\n        ['Portfolio เริ่มต้น', f'{init_port:,.0f} USD'],\n        ['',''],\n        ['── Uptrend Criteria ──',''],\n        ['C1: Close', f'>= {C1_MIN*100:.0f}% R55'],\n        ['C2: Recent Low (5 bars)', f'>= {C2_MIN*100:.0f}% R55'],\n        ['C3: Drop from HH', f'<= {C3_MAX*100:.0f}% R55'],\n        ['C4: Bullish Impulse', f'>= {C4_MIN*100:.0f}% R55'],\n        ['C5: Lookback', 'ไม่มีแท่ง Bearish > 30%R55'],\n        ['C6: ราคาต้น window', f'<= {C6_MAX_START*100:.0f}% R55'],\n        ['',''],\n        ['── Downtrend Criteria (Mirror) ──',''],\n        ['C1D: Close', f'<= {(1-C1_MIN)*100:.0f}% R55 จากบน'],\n        ['C2D: Recent High (5 bars)', f'<= {(1-C2_MIN)*100:.0f}% R55 จากบน'],\n        ['C3D: Rise from LL', f'<= {C3_MAX*100:.0f}% R55'],\n        ['C4D: Bearish Impulse', f'>= {C4_MIN*100:.0f}% R55'],\n        ['C5D: Lookback', 'ไม่มีแท่ง Bullish > 30%R55'],\n        ['',''],\n        ['── Entry ──',''],\n        ['Zone UP',   'Swing Low ใน 65-85% R55'],\n        ['Zone DOWN', 'Swing High ใน 15-35% R55'],\n        ['Buffer',    '100 pip fixed'],\n        ['',''],\n        ['── TP / SL ──',''],\n        ['TP', f'Entry + 50% × (Swing High − Entry)'],\n        ['SL', 'Symmetric กับ TP'],\n        ['R:R','1.00 (symmetric)'],\n        ['',''],\n        ['── Risk ──',''],\n        ['Lot', 'Portfolio × 10% ÷ SL(pip)'],\n        ['Anti-trend', 'detect_down/up_father (40-100% R55 ใน 1-8 แท่ง)'],\n        ['Stop segment on loss', 'แพ้ใน segment ใด → หยุดเทรด segment นั้น'],\n    ]\n\n    # ── Sheet 9: Equity Curve ─────────────────────────────────────\n    eq = init_port\n    rows_eq = [['#','วันที่','เวลา','DIR','ผล','pip','USD','Portfolio (USD)','กำไร% จากต้น']]\n    rows_eq.append([0,'เริ่มต้น','','','','','', round(init_port,2),'0.00%'])\n    for i, t in enumerate(all_trades, 1):\n        if t['result']!='OPEN': eq += t.get('usd',0)\n        pct = (eq-init_port)/init_port*100\n        dt = parse_dt(t.get('entry_time',''))\n        rows_eq.append([\n            i,\n            dt.strftime('%Y-%m-%d') if dt else '',\n            t.get('entry_time','')[-5:],\n            t['direction'],\n            t['result'],\n            round(t.get('pip',0),1),\n            round(t.get('usd',0),2),\n            round(eq,2),\n            f'{pct:.2f}%',\n        ])\n\n    # ── รวม sheet_defs ───────────────────────────────────────────\n    sheet_defs = [\n        {'title':'รายการเทรด',     'data': hdr1 + rows1},\n        {'title':'สรุปรวม',        'data': rows2},\n        {'title':'แยก UP-DOWN',    'data': hdr3 + rows3},\n        {'title':'รายวัน',         'data': hdr4 + rows4},\n        {'title':'รายเดือน',       'data': hdr5 + rows5},\n        {'title':'รายวันสัปดาห์',  'data': hdr6 + rows6},\n        {'title':'Session',        'data': hdr7 + rows7},\n        {'title':'เงื่อนไข',       'data': rows_cond},\n        {'title':'Equity Curve',   'data': rows_eq},\n    ]\n\n    # สร้าง tabs\n    add_reqs = [{'updateSheetProperties':{\n        'properties':{'sheetId':0,'title':sheet_defs[0]['title']},\n        'fields':'title'}}]\n    for i, sd in enumerate(sheet_defs[1:], 1):\n        add_reqs.append({'addSheet':{'properties':{'title':sd['title'],'index':i}}})\n    sheets.spreadsheets().batchUpdate(spreadsheetId=sid,\n        body={'requests':add_reqs}).execute()\n\n    meta    = sheets.spreadsheets().get(spreadsheetId=sid).execute()\n    sid_map = {s['properties']['title']:s['properties']['sheetId']\n               for s in meta['sheets']}\n\n    # เขียนข้อมูล — เขียนทีละ sheet เพื่อง่ายต่อ debug\n    ok_count = 0\n    for sd in sheet_defs:\n        if not sd['data']: continue\n        try:\n            cleaned = []\n            for row in sd['data']:\n                cleaned_row = []\n                for c in row:\n                    if c is None or c == '':\n                        cleaned_row.append('')\n                    elif isinstance(c, bool):\n                        cleaned_row.append(str(c))\n                    elif isinstance(c, (int, float)):\n                        cleaned_row.append(c)\n                    else:\n                        cleaned_row.append(str(c))\n                cleaned.append(cleaned_row)\n            range_name = \"'\" + sd['title'] + \"'!A1\"\n            sheets.spreadsheets().values().update(\n                spreadsheetId=sid,\n                range=range_name,\n                valueInputOption='USER_ENTERED',\n                body={'values': cleaned}\n            ).execute()\n            ok_count += 1\n        except Exception as _e:\n            print(f'⚠️  เขียน [{sd[\"title\"]}] ล้มเหลว: {_e}')\n    print(f'✅ เขียนข้อมูลสำเร็จ {ok_count}/{len(sheet_defs)} sheets')\n\n    # Formatting\n    fmt_reqs = []\n    trade_shid = sid_map.get('รายการเทรด', 0)\n\n    for sd in sheet_defs:\n        shid = sid_map.get(sd['title'], 0)\n        n    = len(sd['data'])\n        # Header สีน้ำเงิน\n        fmt_reqs.append({'repeatCell':{\n            'range':{'sheetId':shid,'startRowIndex':0,'endRowIndex':1},\n            'cell':{'userEnteredFormat':{\n                'backgroundColor':{'red':0.20,'green':0.47,'blue':0.75},\n                'textFormat':{'bold':True,'foregroundColor':{'red':1,'green':1,'blue':1}},\n                'horizontalAlignment':'CENTER'}},\n            'fields':'userEnteredFormat(backgroundColor,textFormat,horizontalAlignment)'}})\n        # Data center\n        fmt_reqs.append({'repeatCell':{\n            'range':{'sheetId':shid,'startRowIndex':1,'endRowIndex':n},\n            'cell':{'userEnteredFormat':{'horizontalAlignment':'CENTER','verticalAlignment':'MIDDLE'}},\n            'fields':'userEnteredFormat(horizontalAlignment,verticalAlignment)'}})\n        # Freeze row 1\n        fmt_reqs.append({'updateSheetProperties':{\n            'properties':{'sheetId':shid,'gridProperties':{'frozenRowCount':1}},\n            'fields':'gridProperties.frozenRowCount'}})\n\n    # สีแถวตามผล + Direction\n    for row_idx, t in enumerate(all_trades, start=1):\n        if t['result']=='WIN' and t['direction']=='UP':\n            bg = {'red':0.18,'green':0.65,'blue':0.32}   # 🟢 เขียวเข้ม — WIN UP\n        elif t['result']=='WIN' and t['direction']=='DOWN':\n            bg = {'red':0.24,'green':0.52,'blue':0.95}   # 🔵 ฟ้าเข้ม — WIN DOWN\n        elif t['result']=='LOSS':\n            bg = {'red':0.0,'green':0.0,'blue':0.0}      # ⬛ ดำ — LOSS\n        else:\n            bg = {'red':1.0,'green':0.85,'blue':0.20}    # 🟡 เหลืองสด — OPEN\n        cell_fmt = {'backgroundColor': bg}\n        if t['result'] == 'LOSS':\n            cell_fmt['textFormat'] = {\n                'bold': True,\n                'foregroundColor': {'red':1.0,'green':1.0,'blue':1.0}\n            }\n            fields = 'userEnteredFormat(backgroundColor,textFormat)'\n        else:\n            fields = 'userEnteredFormat(backgroundColor)'\n        fmt_reqs.append({'repeatCell':{\n            'range':{'sheetId':trade_shid,'startRowIndex':row_idx,'endRowIndex':row_idx+1},\n            'cell':{'userEnteredFormat':cell_fmt},\n            'fields':fields}})\n\n    sheets.spreadsheets().batchUpdate(spreadsheetId=sid,\n        body={'requests':fmt_reqs}).execute()\n\n    # Equity Curve charts (USD + %)\n    eq_shid = sid_map.get('Equity Curve',0)\n    n_eq    = len(rows_eq)\n    sheets.spreadsheets().batchUpdate(spreadsheetId=sid, body={'requests':[\n        # กราฟ 1: Portfolio (USD)\n        {'addChart':{'chart':{\n            'spec':{'title':f'Equity Curve — Portfolio (USD)  [เริ่ม {init_port:,.0f} USD]',\n                'basicChart':{'chartType':'LINE','legendPosition':'BOTTOM_LEGEND',\n                    'axis':[{'position':'BOTTOM_AXIS','title':'ไม้ที่'},\n                             {'position':'LEFT_AXIS','title':'Portfolio (USD)'}],\n                    'domains':[{'domain':{'sourceRange':{'sources':[{\n                        'sheetId':eq_shid,'startRowIndex':0,'endRowIndex':n_eq,\n                        'startColumnIndex':0,'endColumnIndex':1}]}}}],\n                    'series':[{'series':{'sourceRange':{'sources':[{\n                        'sheetId':eq_shid,'startRowIndex':0,'endRowIndex':n_eq,\n                        'startColumnIndex':7,'endColumnIndex':8}]}},\n                        'targetAxis':'LEFT_AXIS'}],\n                    'interpolateNulls':True}},\n            'position':{'overlayPosition':{\n                'anchorCell':{'sheetId':eq_shid,'rowIndex':1,'columnIndex':10},\n                'widthPixels':680,'heightPixels':360}}}}},\n        # กราฟ 2: กำไร% จากต้น\n        {'addChart':{'chart':{\n            'spec':{'title':'Equity Curve — กำไร% จากต้น',\n                'basicChart':{'chartType':'LINE','legendPosition':'BOTTOM_LEGEND',\n                    'axis':[{'position':'BOTTOM_AXIS','title':'ไม้ที่'},\n                             {'position':'LEFT_AXIS','title':'%'}],\n                    'domains':[{'domain':{'sourceRange':{'sources':[{\n                        'sheetId':eq_shid,'startRowIndex':0,'endRowIndex':n_eq,\n                        'startColumnIndex':0,'endColumnIndex':1}]}}}],\n                    'series':[{'series':{'sourceRange':{'sources':[{\n                        'sheetId':eq_shid,'startRowIndex':0,'endRowIndex':n_eq,\n                        'startColumnIndex':8,'endColumnIndex':9}]}},\n                        'targetAxis':'LEFT_AXIS'}],\n                    'interpolateNulls':True}},\n            'position':{'overlayPosition':{\n                'anchorCell':{'sheetId':eq_shid,'rowIndex':22,'columnIndex':10},\n                'widthPixels':680,'heightPixels':360}}}}},\n    ]}).execute()\n\n    # Daily Equity Curve chart\n    day_shid = sid_map.get('รายวัน', 0)\n    n_day = len(rows4) + 1  # +1 for header\n    if n_day > 2:\n        sheets.spreadsheets().batchUpdate(spreadsheetId=sid, body={'requests':[\n            {'addChart':{'chart':{\n                'spec':{'title':'Daily Equity Curve — Portfolio (USD)',\n                    'basicChart':{'chartType':'LINE','legendPosition':'BOTTOM_LEGEND',\n                        'axis':[{'position':'BOTTOM_AXIS','title':'วันที่'},\n                                 {'position':'LEFT_AXIS','title':'Portfolio (USD)'}],\n                        'domains':[{'domain':{'sourceRange':{'sources':[{\n                            'sheetId':day_shid,'startRowIndex':1,'endRowIndex':n_day,\n                            'startColumnIndex':0,'endColumnIndex':1}]}}}],\n                        'series':[{'series':{'sourceRange':{'sources':[{\n                            'sheetId':day_shid,'startRowIndex':1,'endRowIndex':n_day,\n                            'startColumnIndex':6,'endColumnIndex':7}]}},\n                            'targetAxis':'LEFT_AXIS'}],\n                        'interpolateNulls':True}},\n                'position':{'overlayPosition':{\n                    'anchorCell':{'sheetId':day_shid,'rowIndex':1,'columnIndex':8},\n                    'widthPixels':680,'heightPixels':360}}}}}\n        ]}).execute()\n\n    print(f\"\\n✅ Export สำเร็จ — {sheet_title}\")\n    print(f\"   🔗 {url}\")\n    return url\n\n\n# ── รัน Export ──────────────────────────────────────────────\nif EXPORT_GSHEET:\n    if 'trades_up' in dir() and (trades_up or trades_down):\n        print('=' * 60)\n        print('  📊 กำลังสร้าง Google Sheet ...')\n        print('=' * 60)\n        sheet_url = export_to_gsheet(\n            trades_up    = trades_up,\n            trades_down  = trades_down,\n            csv_filename = CSV_PATH,\n            version      = VERSION,\n        )\n        if sheet_url:\n            print(f'\\n🔗 เปิด Sheet: {sheet_url}')\n    else:\n        print('\\n⚠️  รัน Cell 4 ก่อน เพื่อให้มี trades_up / trades_down')\nelse:\n    print('\\nℹ️  EXPORT_GSHEET = False — ข้าม Export')\n"
   ],
   "metadata": {},
   "outputs": [],
   "execution_count": null
  }
 ]
}